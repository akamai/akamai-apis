name: activate
in: query
description: |-
  Specify if you want to activate the modified configuration, `false` by default. Otherwise, you can run the [Activate a configuration](ref:post-configuration-activations) operation separately.
  
x-akamai:
  file-path: parameters/activate-query.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
