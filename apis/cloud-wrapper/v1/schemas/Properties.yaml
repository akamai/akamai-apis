type: object
description: |-
  Eligible properties that don't have Cloud Wrapper enabled.
  
x-akamai:
  file-path: schemas/Properties.yaml
additionalProperties: false
required:
- properties
properties:
  properties:
    type: array
    description: |
      A list of all eligible properties that don't have Cloud Wrapper enabled.
    minItems: 0
    items:
      $ref: ../schemas/Property.yaml
