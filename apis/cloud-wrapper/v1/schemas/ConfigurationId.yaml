type: object
x-akamai:
  file-path: schemas/ConfigurationId.yaml
required:
- configId
properties:
  configId:
    type: integer
    description: |
      Unique id associated with the Cloud Wrapper configuration.
    format: int64
