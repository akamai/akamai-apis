type: object
description: |-
  Auth keys for a specified CDN.
  
x-akamai:
  file-path: schemas/CdnAuthKeys.yaml
additionalProperties: false
required:
- cdnAuthKeys
properties:
  cdnAuthKeys:
    type: array
    description: |
      List of auth keys for the specified CDN.
    minItems: 0
    items:
      $ref: ../schemas/CdnAuthKey.yaml
