type: object
x-akamai:
  file-path: schemas/BaseDirectory.yaml
additionalProperties: false
required:
- value
nullable: true
properties:
  value:
    type: string
    description: |
      Specifies the base path of content on your origin server. The value needs to begin and end with a slash (/) character.
    pattern: ^/.*/$
