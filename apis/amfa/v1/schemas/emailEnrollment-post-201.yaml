type: object
x-akamai:
  file-path: schemas/emailEnrollment-post-201.yaml
required:
- userIds
properties:
  successfullySent:
    type: array
    description: |
      A set of `userIds` for user accounts to which the enrollment emails were succesfully sent.
    minItems: 0
    items:
      $ref: common-definitions.yaml#/definitions/nonEmptyString
