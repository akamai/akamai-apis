type: object
description: |
  Specifies the user's alias.
additionalProperties: false
required:
- alias
properties:
  alias:
    description: |
      Alternate usernames for the user.
    $ref: common-definitions.yaml#/definitions/nonEmptyString
