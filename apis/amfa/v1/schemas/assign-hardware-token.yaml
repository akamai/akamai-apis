type: object
description: |
  Specifies a hardware token to assign to a user account.
additionalProperties: false
required:
- deviceId
properties:
  deviceId:
    description: |
      A unique identifier for the hardware token. You can assign only one token to a specific user account.
    $ref: common-definitions.yaml#/definitions/nonEmptyString
