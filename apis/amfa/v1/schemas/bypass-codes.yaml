type: object
description: |
  Provides response data with bypass codes generated for a specific user account.
additionalProperties: false
required:
- bypassCodes
properties:
  bypassCodes:
    type: array
    description: |
      Bypass codes generated for a specific user account.
    minItems: 0
    items:
      $ref: bypass-code-get-200.yaml
