type: object
required:
- userIds
properties:
  successfullySent:
    type: array
    description: |
      A set of `userIds` for user accounts to which the enrollment emails were successfully sent.
    minItems: 0
    items:
      $ref: common-definitions.yaml#/definitions/nonEmptyString
