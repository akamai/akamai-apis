x-akamai:
  path-info: /users/{userId}/bypass-codes/{bypassCodeId}
parameters:
- $ref: ../parameters/user-id-path.yaml
- $ref: ../parameters/bypass-code-id-path.yaml
get:
  operationId: get-bypass-code
  summary: Get a bypass code
  tags:
  - Bypass codes
  description: |
    Get a specific bypass code.
  parameters:
  - $ref: ../parameters/contract-id-required.yaml
  responses:
    200:
      description: |
        The operation provides data for a specific bypass code.
      content:
        application/json:
          example:
            $ref: ../examples/bypass-code-get-200.json
          schema:
            $ref: ../schemas/bypass-code-get-200.yaml
    401:
      $ref: ../errors/401.yaml
    404:
      $ref: ../errors/404.yaml
    500:
      $ref: ../errors/500.yaml
