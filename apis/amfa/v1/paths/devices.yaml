x-akamai:
  path-info: /users/{userId}/devices
parameters:
- $ref: ../parameters/user-id-path.yaml
get:
  operationId: get-user-devices
  summary: List the user's devices
  tags:
  - Devices
  description: |
    List all the user's authentication devices.
  parameters:
  - $ref: ../parameters/contract-id-required.yaml
  responses:
    200:
      description: |
        The response provides a list of the user's devices.
      content:
        application/json:
          example:
            $ref: ../examples/devices-get-200.json
          schema:
            $ref: ../schemas/devices.yaml
    401:
      $ref: ../errors/401.yaml
    404:
      $ref: ../errors/404.yaml
    500:
      $ref: ../errors/500.yaml
