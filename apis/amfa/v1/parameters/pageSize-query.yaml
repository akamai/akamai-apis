name: pageSize
in: query
description: |-
  The number of user records displayed on each page. The default value is `25`.
  
x-akamai:
  file-path: parameters/pageSize-query.yaml
schema:
  type: integer
  example: 50
  default: 25
