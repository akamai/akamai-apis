name: status
in: query
description: |
  Filter user records by the specific status and display a list of user accounts that have this status.
required: false
schema:
  type: string
  example: ACTIVE
  enum:
  - ACTIVE
  - UNENROLLED
  - DEVICES_DISABLED
  - PROVISIONING_DISABLED
