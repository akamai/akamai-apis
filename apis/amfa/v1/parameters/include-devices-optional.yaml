name: includeDevices
in: query
description: |
  Whether the response should include device details for listed users. The default value is `false`.
schema:
  type: boolean
  example: true
  default: false
