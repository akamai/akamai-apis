name: size
in: query
description: |-
  Specifies the number of objects on a page, at least `10` objects.
  
x-akamai:
  file-path: parameters/size-query.yaml
required: true
schema:
  type: integer
  example: 100
  minimum: 10
