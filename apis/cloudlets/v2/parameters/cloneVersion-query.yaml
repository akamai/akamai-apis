name: cloneVersion
in: query
description: |-
  If cloning an existing policy version, this parameter value is the number of the policy version you want to clone. If there are attributes that you do _not_ want propagated from the source policy version, you must provide the new values in the request.
  
x-akamai:
  file-path: parameters/cloneVersion-query.yaml
schema:
  type: number
