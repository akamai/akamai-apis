name: network
in: path
description: |-
  Returns only activations for the selected network, either `staging` or `production`.
  
x-akamai:
  file-path: parameters/network-path.yaml
required: true
schema:
  type: string
