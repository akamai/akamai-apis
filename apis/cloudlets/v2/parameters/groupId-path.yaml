name: groupId
in: path
description: |-
  Get a group: For GET operations, returns only policies associated with the group ID entered. Enter `0` to get policies in all groups.
  
x-akamai:
  file-path: parameters/groupId-path.yaml
required: true
schema:
  type: number
