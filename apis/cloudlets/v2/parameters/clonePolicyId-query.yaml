name: clonePolicyId
in: query
description: |-
  If cloning an existing policy, this parameter is the ID of the policy (`policyId`) you want to clone. If there are attributes that you do _not_ want propagated from the source policy, you must provide the new values in the request.
  
x-akamai:
  file-path: parameters/clonePolicyId-query.yaml
schema:
  type: number
