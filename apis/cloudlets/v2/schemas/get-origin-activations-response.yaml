type: array
description: |
  List of current load balancing activations on `STAGING`, and `PRODUCTION` networks.  
x-akamai:
  file-path: schemas/get-origin-activations-response.yaml
items:
  $ref: origin-activation.yaml
