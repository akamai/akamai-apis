description: |
  Limit reported data per IP version, `ipv4`, `ipv6`, or the default `all` for either version.
in: query
name: ipVersion
schema:
  default: all
  enum:
  - ipv4
  - ipv6
  - all
  type: string
  example: all
