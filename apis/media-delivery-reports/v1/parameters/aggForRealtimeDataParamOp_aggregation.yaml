name: aggregation
in: query
description: |-
  The time period to group each data record, in seconds. Acceptable values include `300` (five minutes), `3600` (one hour), and `86400` (one day).
  
x-akamai:
  file-path: parameters/aggForRealtimeDataParamOp_aggregation.yaml
schema:
  type: integer
  example: 3600
  default: 86400
  enum:
  - 300
  - 3600
  - 86400
