description: |
  Limit reported data to `encrypted` or `unencrypted` media, or specify the default `all` for unlimited data.
in: query
name: mediaEncryption
schema:
  default: all
  enum:
  - encrypted
  - unencrypted
  - all
  type: string
  example: all
