name: metrics
in: query
description: |-
  Specify the metrics to use for the report, as comma-separated IDs. You need to include at least the `metrics` or `dimensions` parameter, but you can include both if desired. See the table of available ACE [metrics](doc:rpt-metrics-ace#the-metrics) which includes their API-specific `id` values.
  
x-akamai:
  file-path: parameters/metrics-ace-query.yaml
required: true
schema:
  type: string
  example: 1,2,3
