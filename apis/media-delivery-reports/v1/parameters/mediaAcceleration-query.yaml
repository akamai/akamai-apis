name: mediaAcceleration
in: query
description: |-
  Limit reported data to media that's `accelerated` or `nonaccelerated`, or specify the default `all` for unlimited data.
  
x-akamai:
  file-path: parameters/mediaAcceleration-query.yaml
schema:
  type: string
  example: all
  default: all
  enum:
  - accelerated
  - nonaccelerated
  - all
