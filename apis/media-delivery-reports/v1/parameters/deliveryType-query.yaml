description: |
  Limit data to the context of media delivery, `live`, `vod` (video on demand), or the default `all`.
in: query
name: deliveryType
schema:
  default: all
  enum:
  - live
  - vod
  - all
  type: string
  example: all
