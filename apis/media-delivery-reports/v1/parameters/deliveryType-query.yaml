name: deliveryType
in: query
description: |-
  Limit data to the context of media delivery, `live`, `vod` (video on demand), or the default `all`.
  
x-akamai:
  file-path: parameters/deliveryType-query.yaml
schema:
  type: string
  example: all
  default: all
  enum:
  - live
  - vod
  - all
