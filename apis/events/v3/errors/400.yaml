description: |
  [Bad request](ref:400). Invalid request. Details may be available in the response body.
content:
  application/problem+json:
    example:
      $ref: ../examples/bad-request-400.json
    schema:
      $ref: ../schemas/problem.yaml
