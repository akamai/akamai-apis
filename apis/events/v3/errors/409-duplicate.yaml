description: |
  [Conflict](ref:409). An event with same name, start and end already exists.
content:
  application/problem+json:
    example:
      $ref: ../examples/duplicate-409.json
    schema:
      $ref: ../schemas/problem.yaml
