description: |
  [Forbidden](ref:403). Insufficient permissions to access event or associated objects.
content:
  application/problem+json:
    example:
      $ref: ../examples/forbidden-event-403.json
    schema:
      $ref: ../schemas/problem.yaml
