description: |
  [Conflict](ref:409). The event was modified after you retrieved it. Please refetch the event, reconcile possible differences and resubmit the request.
content:
  application/problem+json:
    example:
      $ref: ../examples/concurrent-modification-409.json
    schema:
      $ref: ../schemas/problem.yaml
