description: |
  [Not found](ref:404). The requested event wasn't found.
content:
  application/problem+json:
    example:
      $ref: ../examples/not-found-404.json
    schema:
      $ref: ../schemas/problem.yaml
