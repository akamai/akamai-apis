type: object
description: |-
  Contains information about a Quality of Service (QoS) report pack associated with the event.
  
x-akamai:
  file-path: schemas/qos-report-pack.yaml
title: QoS report pack
additionalProperties: false
required:
- id
- type
- name
properties:
  id:
    type: integer
    description: |
      The identifier of the QoS report pack.
  name:
    type: string
    description: |
      The name of the QoS report pack.
  type:
    type: string
    description: |
      The type of object. `QOS_REPORT_PACK` in this case.
    enum:
    - QOS_REPORT_PACK
