openapi: 3.0.0
info:
  title: Events
  version: v3
servers:
- url: https://{hostname}/events/v3
x-akamai:
  auth-type: EDGE_GRID
paths:
  /events:
    $ref: paths/events.yaml
  /events/{eventId}:
    $ref: paths/event.yaml
