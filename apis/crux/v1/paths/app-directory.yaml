x-akamai:
  file-path: paths/app-directory.yaml
  path-info: /mgmt-pop/appdirectories/{appDirectoryId}
parameters:
- $ref: ../parameters/app-directory-id-path.yaml
get:
  operationId: get-app-directory
  summary: Get an application directory
  tags:
  - Application directories
  description: |
    Get details for a specific application directory.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  - $ref: ../parameters/expand-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-an-app-directory-200.json
          schema:
            $ref: ../schemas/app-directory.yaml
