x-akamai:
  file-path: paths/test-crowdstrike.yaml
  path-info: /device-posture/risk-assessment/partner/crowdstrike/test
post:
  operationId: post-partner-crowdstrike-test
  summary: Create a CrowdStrike integration
  tags:
  - Risk assessment
  description: |
    Create a CrowdStrike integration to test against your device posture configuration.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  requestBody:
    required: true
    content:
      application/json:
        example:
          $ref: ../examples/post-risk-assessment-partner-crowdstrike-200.json
        schema:
          $ref: ../schemas/crowdstrike-integration.yaml
  responses:
    200:
      description: |
        Successful response.
