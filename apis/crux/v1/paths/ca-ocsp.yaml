x-akamai:
  file-path: paths/ca-ocsp.yaml
  path-info: /device-posture/risk-assessment/ca-ocsp
get:
  operationId: get-ca-ocsp
  summary: Get CA and OCSP information
  tags:
  - Risk assessment
  description: |
    Get CA and OCSP information for use in certificate profiles.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-ca-ocsp-200.json
          schema:
            $ref: ../schemas/all-ca-ocsp.yaml
