x-akamai:
  file-path: paths/directories-membership.yaml
  path-info: /mgmt-pop/idp/{idp_id}/directories_membership
parameters:
- $ref: ../parameters/idp-id-path.yaml
get:
  operationId: get-idp-directories-membership
  summary: Get an IDP directory membership
  tags:
  - Directory membership
  description: |
    Retrieves associated directory membership of an IDP.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-idp-directories-membership-200.json
          schema:
            $ref: ../schemas/idp-directories-memberships.yaml
