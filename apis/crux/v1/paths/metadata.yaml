x-akamai:
  file-path: paths/metadata.yaml
  path-info: /mgmt-pop/idp/{idp_id}/metadata
parameters:
- $ref: ../parameters/idp-id-path.yaml
post:
  operationId: post-idp-metadata
  summary: Upload metadata of an IDP
  tags:
  - Metadata
  description: |
    Uploads metadata to add or update to the IDP.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            metadata: '...'
          schema:
            $ref: ../schemas/idp-metadata.yaml
