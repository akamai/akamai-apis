x-akamai:
  file-path: paths/device-biometric.yaml
  path-info: /device-posture/inventory/summary/device-biometric
get:
  operationId: get-device-biometric
  summary: List devices with biometrics
  tags:
  - Device inventory
  description: |
    Lists activity metrics for biometrics signals acquired from devices accessing your applications. The response groups statuses by `value` and sums by `count`.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      $ref: ../errors/signal-summary-request-200.yaml
    400:
      $ref: ../errors/400.yaml
    401:
      $ref: ../errors/401.yaml
    403:
      $ref: ../errors/403.yaml
    404:
      $ref: ../errors/404.yaml
    405:
      $ref: ../errors/405.yaml
    415:
      $ref: ../errors/415.yaml
