x-akamai:
  file-path: paths/system-metrics.yaml
  path-info: /mgmt-pop/agents/{uuid_url}/system_resource/metrics
parameters:
- $ref: ../parameters/uuid-url-path.yaml
get:
  operationId: get-connector-system-metrics
  summary: List connector system metrics
  tags:
  - System metrics
  description: |
    Lists system metrics for the specified connector.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  - $ref: ../parameters/system-metrics-period-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/agent-system-metrics-200.json
          schema:
            $ref: ../schemas/agentsystemmetrics.yaml
