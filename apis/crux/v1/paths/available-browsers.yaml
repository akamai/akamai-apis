x-akamai:
  file-path: paths/available-browsers.yaml
  path-info: /device-posture/risk-assessment/available-browsers
get:
  operationId: get-available-browsers
  summary: List available browser versions
  tags:
  - Risk assessment
  description: |
    Lists supported browser versions for risk assessment. Unlisted browsers aren't supported for risk assessment.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-available-browsers-200.json
          schema:
            $ref: ../schemas/available-browser-ver.yaml
