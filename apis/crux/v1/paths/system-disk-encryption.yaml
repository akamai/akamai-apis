x-akamai:
  file-path: paths/system-disk-encryption.yaml
  path-info: /device-posture/inventory/summary/system-disk-encryption
get:
  operationId: get-disk-encryption
  summary: List system disk encryption
  tags:
  - Device inventory
  description: |
    Lists activity metrics for disk encryption signals acquired from devices accessing your applications. The response groups statuses by `value` and sums by `count`.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      $ref: ../errors/signal-summary-request-200.yaml
