x-akamai:
  file-path: paths/approve.yaml
  path-info: /mgmt-pop/agents/{uuid_url}/approve
parameters:
- $ref: ../parameters/uuid-url-path.yaml
post:
  operationId: post-connector-approve
  summary: Approve a connector
  tags:
  - Connectors
  description: |
    Approves a connector.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/approve-agent-200.json
          schema:
            $ref: ../schemas/agent-approve.yaml
