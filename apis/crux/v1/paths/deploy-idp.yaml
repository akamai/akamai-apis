x-akamai:
  file-path: paths/deploy-idp.yaml
  path-info: /mgmt-pop/idp/{idp_id}/deploy
parameters:
- $ref: ../parameters/idp-id-path.yaml
post:
  operationId: post-idp-deploy
  summary: Deploy an IDP
  tags:
  - IDPs
  description: |
    Deploys an IDP.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/deploy-idp-200.json
          schema:
            $ref: ../schemas/idp-cmdid.yaml
