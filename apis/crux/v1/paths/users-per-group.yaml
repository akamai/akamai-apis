x-akamai:
  file-path: paths/users-per-group.yaml
  path-info: /mgmt-pop/directories/{directory_id}/groups/{group_id}/users
parameters:
- $ref: ../parameters/group-id-path.yaml
- $ref: ../parameters/directory-id-path.yaml
get:
  operationId: get-directory-group-users
  summary: List directory group users
  tags:
  - Directory groups
  description: |
    Lists users in a directory group.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-group-users-200.json
          schema:
            $ref: ../schemas/group-users.yaml
