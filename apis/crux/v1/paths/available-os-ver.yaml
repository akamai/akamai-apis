x-akamai:
  file-path: paths/available-os-ver.yaml
  path-info: /device-posture/risk-assessment/available-os-ver
get:
  operationId: get-os-versions
  summary: List available operating system versions
  tags:
  - Risk assessment
  description: |
    Lists supported operating system versions  for risk assessment. Unlisted versions aren't supported for risk assessment.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-available-os-ver-200.json
          schema:
            $ref: ../schemas/available-os-ver.yaml
