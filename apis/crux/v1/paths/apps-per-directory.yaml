x-akamai:
  file-path: paths/apps-per-directory.yaml
  path-info: /mgmt-pop/directories/{directory_id}/apps
parameters:
- $ref: ../parameters/directory-id-path.yaml
get:
  operationId: get-directory-apps
  summary: List directory applications
  tags:
  - Applications
  description: |
    Lists applications using the directory.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-directory-apps-200.json
          schema:
            $ref: ../schemas/applications.yaml
