x-akamai:
  file-path: paths/cmd-verify.yaml
  path-info: /mgmt-pop/directories/verify/{cmd_id}
parameters:
- $ref: ../parameters/cmd-id-path.yaml
get:
  operationId: get-verify-status
  summary: Check directory verification status
  tags:
  - Directory administration
  description: |
    Checks the directory verification status.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/check-verify-status-200.json
          schema:
            $ref: ../schemas/status.yaml
