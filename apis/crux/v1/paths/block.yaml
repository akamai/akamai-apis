x-akamai:
  file-path: paths/block.yaml
  path-info: /mgmt-pop/idpblockuser/idp/{idp_id}/block
parameters:
- $ref: ../parameters/idp-id-path.yaml
post:
  operationId: post-user-block-idp-access
  summary: Block a user from accessing an IDP
  tags:
  - Block user
  description: |
    Blocks a user from accessing an IDP.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  requestBody:
    required: true
    content:
      application/json:
        example:
          $ref: ../examples/idp-block-user-post-request.json
        schema:
          $ref: ../schemas/idp-user-block-unblock-request.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/idp-user-block-unblock-response.json
          schema:
            $ref: ../schemas/idp-user-block-unblock-response.yaml
