x-akamai:
  file-path: paths/os-names.yaml
  path-info: /device-posture/risk-assessment/os-names
get:
  operationId: get-os-names
  summary: List available operating systems
  tags:
  - Risk assessment
  description: |
    Lists supported operating systems for risk assessment. Unlisted operating systems aren't supported for risk assessment.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-available-os-200.json
          schema:
            $ref: ../schemas/available-os.yaml
