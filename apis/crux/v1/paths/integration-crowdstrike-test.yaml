x-akamai:
  file-path: paths/integration-crowdstrike-test.yaml
  path-info: /device-posture/risk-assessment/partner/crowdstrike/test/{integration_id}
parameters:
- $ref: ../parameters/integration-id-path.yaml
put:
  operationId: put-partner-crowdstrike-test
  summary: Creates a CrowdStrike integration for testing
  tags:
  - Risk assessment
  description: |
    Test an existing CrowdStrike integration against your device posture configuration.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  requestBody:
    required: true
    content:
      application/json:
        example:
          $ref: ../examples/put-risk-assessment-partner-crowdstrike-200.json
        schema:
          $ref: ../schemas/crowdstrike-integration.yaml
  responses:
    200:
      description: |
        Successful response.
