x-akamai:
  file-path: paths/verify.yaml
  path-info: /mgmt-pop/directories/verify
post:
  operationId: post-verify
  summary: Verify a directory
  tags:
  - Directory administration
  description: |
    Verifies that directories are reachable by connectors.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/verify-directory-200.json
          schema:
            $ref: ../schemas/cmdid.yaml
