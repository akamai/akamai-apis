x-akamai:
  file-path: paths/device-jailbroken.yaml
  path-info: /device-posture/inventory/summary/device-jailbroken
get:
  operationId: get-device-jailbroken
  summary: List jailbroken devices
  tags:
  - Device inventory
  description: |
    Lists jailbroken device signal metrics acquired from device application access. The response groups statuses by `value` and sums by `count`.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      $ref: ../errors/signal-summary-request-200.yaml
