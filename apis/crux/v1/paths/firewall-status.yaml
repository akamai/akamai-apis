x-akamai:
  file-path: paths/firewall-status.yaml
  path-info: /device-posture/inventory/summary/firewall-status
get:
  operationId: get-firewall-status
  summary: List firewall status
  tags:
  - Device inventory
  description: |
    Lists activity metrics for firewall status signals acquired from devices accessing your applications. The response groups statuses by `value` and sums by `count`.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      $ref: ../errors/signal-summary-request-200.yaml
