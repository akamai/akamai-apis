x-akamai:
  file-path: paths/app-group.yaml
  path-info: /mgmt-pop/appgroups/{appGroupId}
parameters:
- $ref: ../parameters/app-group-id-path.yaml
get:
  operationId: get-app-group
  summary: Get an application group
  tags:
  - Application groups
  description: |
    Get details for an application group.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  - $ref: ../parameters/expand-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-an-app-group-200.json
          schema:
            $ref: ../schemas/app-group.yaml
