x-akamai:
  file-path: paths/g2o-per-idp.yaml
  path-info: /mgmt-pop/idp/{idp_id}/g2o
parameters:
- $ref: ../parameters/idp-id-path.yaml
post:
  operationId: post-idp-g2o
  summary: Enable Ghost to Origin (G2O) for an IDP
  tags:
  - Ghost to Origin
  description: |
    Enable G2O for an IDP.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/create-an-g2o-idp-body-200.json
          schema:
            $ref: ../schemas/g2o.yaml
