x-akamai:
  file-path: paths/signal-names.yaml
  path-info: /device-posture/risk-assessment/signal-names
get:
  operationId: get-signal-names
  summary: List signal names
  tags:
  - Risk assessment
  description: |
    Lists supported signal names for risk assessment. Unlisted signals aren't supported for risk assessment.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-signal-names-200.json
          schema:
            $ref: ../schemas/signal-names.yaml
