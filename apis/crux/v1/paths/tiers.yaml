x-akamai:
  file-path: paths/tiers.yaml
  path-info: /device-posture/risk-assessment/tiers
get:
  operationId: get-tiers
  summary: List risk tiers
  tags:
  - Risk assessment
  description: |
    Lists supported risk tiers for risk assessment.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-risk-assessment-tiers-200.json
          schema:
            $ref: ../schemas/risk-rules.yaml
