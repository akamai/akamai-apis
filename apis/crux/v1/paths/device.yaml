x-akamai:
  file-path: paths/device.yaml
  path-info: /device-posture/inventory/device-history/{deviceId}
parameters:
- $ref: ../parameters/device-id-path.yaml
get:
  operationId: get-device-history
  summary: Get device history
  tags:
  - Device history
  description: |
    Lists device history for a particular device.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-device-history-reports-200.json
          schema:
            $ref: ../schemas/device-history.yaml
