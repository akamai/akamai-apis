x-akamai:
  file-path: paths/device-roll-ups.yaml
  path-info: /device-posture/inventory/summary/device-roll-ups
get:
  operationId: get-device-roll-ups
  summary: List device activity
  tags:
  - Device inventory
  description: |
    Lists activity metrics of devices connecting  to your applications.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-device-summary-roll-ups-200.json
          schema:
            $ref: ../schemas/roll-ups.yaml
