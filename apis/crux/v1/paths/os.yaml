x-akamai:
  file-path: paths/os.yaml
  path-info: /device-posture/inventory/summary/os
get:
  operationId: get-summary-os
  summary: List operating system activity
  tags:
  - Device inventory
  description: |
    Lists activity metrics for installed operating  systems of devices connecting to your applications.
  parameters:
  - $ref: ../parameters/optional-contract-id-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-device-summary-os-200.json
          schema:
            $ref: ../schemas/operating-systems.yaml
