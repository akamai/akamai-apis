x-akamai:
  file-path: paths/services-per-app.yaml
  path-info: /mgmt-pop/apps/{applicationId}/services
parameters:
- $ref: ../parameters/application-id-path.yaml
get:
  operationId: get-app-services
  summary: List services for an application
  tags:
  - Services
  description: |
    Lists the services for an application.
  parameters:
  - $ref: ../parameters/contract-id-query.yaml
  - $ref: ../parameters/expand-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-services-for-application-200.json
          schema:
            $ref: ../schemas/application-services-response.yaml
