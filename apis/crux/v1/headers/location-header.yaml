description: |-
  The unique identifier for the newly created resource. Append this ID to a subsequent GET call to view the resource details.
  
x-akamai:
  file-path: headers/location-header.yaml
schema:
  type: integer
  example: 1100
