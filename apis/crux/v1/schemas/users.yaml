type: object
description: |-
  Directory users.
  
x-akamai:
  file-path: schemas/users.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Directory users.
    minItems: 1
    items:
      $ref: user.yaml
