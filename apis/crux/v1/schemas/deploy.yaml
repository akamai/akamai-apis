type: object
description: |-
  Enterprise Access Application deployment data.
  
x-akamai:
  file-path: schemas/deploy.yaml
additionalProperties: false
required:
- cmdid
properties:
  cmdid:
    type: string
    description: |
      A unique identifier for the command to track progress.
    minLength: 1
