type: object
description: |-
  Users for bulk deletion.
  
x-akamai:
  file-path: schemas/users-delete-bulk.yaml
additionalProperties: false
required:
- user_list
properties:
  user_list:
    type: array
    description: |
      Users to bulk delete.
    minItems: 1
    items:
      type: string
      minLength: 1
