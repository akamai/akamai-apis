type: object
description: |-
  The ID of the certificate profile created.
  
x-akamai:
  file-path: schemas/certificate-profile-id.yaml
additionalProperties: false
required:
- id
properties:
  id:
    type: integer
    description: |
      The unique identifier of the created certificate profile.
    minimum: 0
