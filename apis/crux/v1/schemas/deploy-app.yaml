type: object
x-akamai:
  file-path: schemas/deploy-app.yaml
additionalProperties: false
required:
- cmdid
properties:
  app_id:
    type: string
    description: |
      The unique application to deploy.
  cmdid:
    type: integer
    description: |
      A unique identifier for the command to track progress.
