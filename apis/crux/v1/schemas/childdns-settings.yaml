type: array
description: |-
  Custom DNS settings.
  
x-akamai:
  file-path: schemas/childdns-settings.yaml
items:
  type: object
  additionalProperties: true
  required:
  - settings
  properties:
    custom_dns:
      type: boolean
      description: |
        Custom child DNS settings.
      default: false
    settings:
      type: string
