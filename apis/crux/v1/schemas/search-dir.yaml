type: object
description: |-
  Directory user search results.
  
x-akamai:
  file-path: schemas/search-dir.yaml
additionalProperties: false
required:
- user
properties:
  user:
    type: string
    description: |
      The directory user.
    minLength: 1
