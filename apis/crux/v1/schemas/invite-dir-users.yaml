type: object
description: |-
  Invite directory IDPs.
  
x-akamai:
  file-path: schemas/invite-dir-users.yaml
additionalProperties: false
required:
- email
- first_name
- last_name
properties:
  email:
    type: string
    description: |
      The user's email address.
  first_name:
    type: string
    description: |
      First name of the user.
    minLength: 1
  last_name:
    type: string
    description: |
      Last name of the user.
    minLength: 1
