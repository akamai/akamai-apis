type: object
description: |-
  Companies.
  
x-akamai:
  file-path: schemas/companies.yaml
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Companies.
    items:
      $ref: company.yaml
