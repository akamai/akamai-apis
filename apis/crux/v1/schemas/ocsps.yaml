type: object
description: |
  OCSPs
x-akamai:
  file-path: schemas/ocsps.yaml
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      OCSP properties.
    items:
      $ref: ocsp.yaml
