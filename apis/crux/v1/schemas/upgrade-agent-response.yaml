type: object
description: |-
  Upgrade the OS packages on an existing connector.
  
x-akamai:
  file-path: schemas/upgrade-agent-response.yaml
additionalProperties: false
required:
- status
properties:
  status:
    type: string
    description: |
      The status of request (whether the upgrade request was accepted or rejected).
    minLength: 1
