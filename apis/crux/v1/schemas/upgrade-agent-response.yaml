type: object
description: |-
  OS packages upgrade status on an existing connector.
  
x-akamai:
  file-path: schemas/upgrade-agent-response.yaml
additionalProperties: false
required:
- status
properties:
  status:
    type: string
    description: |
      The acceptance status of the upgrade request.
    minLength: 1
