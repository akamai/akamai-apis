type: object
description: |-
  Enterprise Access Application categories.
  
x-akamai:
  file-path: schemas/app-categories.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Application category details.
    minItems: 1
    items:
      $ref: app-category.yaml
