type: object
description: |-
  IDP user unblock request.
  
x-akamai:
  file-path: schemas/idp-user-block-unblock-request.yaml
additionalProperties: false
properties:
  user_list:
    type: array
    description: |
      IDP users to unblock.
    items:
      type: object
      description: |
        User properties for each user to unblock.
      additionalProperties: false
      required:
      - user_id
      - directory_id
      properties:
        directory_id:
          type: string
          description: |
            The unique IDP directory ID.
          minLength: 1
        user_id:
          type: string
          description: |
            A unique ID for the user's account.
          minLength: 1
