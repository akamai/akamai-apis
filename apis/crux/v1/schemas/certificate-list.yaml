type: object
description: |-
  Certificates.
  
x-akamai:
  file-path: schemas/certificate-list.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Certificate details.
    minItems: 0
    items:
      $ref: certificate-post.yaml
