type: object
description: |-
  Enterprise connectors.
  
x-akamai:
  file-path: schemas/agents.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    description: |
      Paging data.
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Connector data.
    items:
      $ref: agent.yaml
