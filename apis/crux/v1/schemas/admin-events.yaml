type: array
description: |-
  Admin report details.
  
x-akamai:
  file-path: schemas/admin-events.yaml
items:
  type: object
  additionalProperties: true
  properties:
    additionalProperties:
      description: |
        Incrementing string-formatted integer values for each admin event report.
      $ref: admin-events-nested.yaml
    count:
      type: integer
      description: |
        The number of entries in the admin event report set.
      minimum: 0
