type: object
description: |
  Admin event properties logged for each admin event. 
x-akamai:
  file-path: schemas/admin-events-nested.yaml
additionalProperties: false
required:
- uid
properties:
  event:
    type: string
    description: |
      A description of the event type.
    minLength: 1
  event_ty:
    type: string
    description: |
      The event type.
    minLength: 1
  res_id:
    type: string
    description: |
      The unique ID of the resource.
    minLength: 1
  res_name:
    type: string
    description: |
      The username.
    minLength: 1
  res_ty:
    type: string
    description: |
      The resource type.
    minLength: 1
  tnid:
    type: string
    description: |
      The tenant's username.
    minLength: 1
  ts:
    type: integer
    description: |
      The epoch timestamp of the report.
    minimum: 1
  uid:
    type: string
    description: |
      The user's email address.
