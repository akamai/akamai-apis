type: object
description: |-
  Users to re-invite in bulk.
  
x-akamai:
  file-path: schemas/users-reinvite-bulk.yaml
additionalProperties: false
required:
- user_list
properties:
  user_list:
    type: array
    description: |
      Users to re-invite.
    minItems: 1
    items:
      type: string
      minLength: 1
