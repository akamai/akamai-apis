type: object
description: |-
  OIDC clients.
  
x-akamai:
  file-path: schemas/childdns-list.yaml
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      OIDC clients.
    items:
      $ref: childdns.yaml
