type: object
description: |-
  Add a comment to an app deployment.
  
x-akamai:
  file-path: schemas/deploy-app-comment.yaml
additionalProperties: false
required:
- deploy_note
properties:
  deploy_note:
    type: string
    description: |
      Comment to summarize your app's deployment.
    minLength: 1
