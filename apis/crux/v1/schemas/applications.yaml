type: object
description: |-
  Applications.
  
x-akamai:
  file-path: schemas/applications.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Applications for the account.
    minItems: 1
    items:
      $ref: application.yaml
