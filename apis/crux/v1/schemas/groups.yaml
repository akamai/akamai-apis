type: object
description: |-
  Access groups.
  
x-akamai:
  file-path: schemas/groups.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Groups.
    items:
      $ref: group.yaml
