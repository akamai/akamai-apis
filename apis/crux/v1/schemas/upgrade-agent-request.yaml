type: object
description: |-
  Upgrades the OS packages on an existing connector.
  
x-akamai:
  file-path: schemas/upgrade-agent-request.yaml
additionalProperties: false
required:
- uuid_url
properties:
  uuid_url:
    type: string
    description: |
      The unique ID of the agent.
    minLength: 1
