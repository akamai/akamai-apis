type: object
description: |-
  Synchronization task message.
  
x-akamai:
  file-path: schemas/sync-dir.yaml
additionalProperties: false
required:
- message
properties:
  message:
    type: string
    description: |
      A message about the directory sync operation.
    minLength: 1
