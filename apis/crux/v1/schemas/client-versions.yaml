type: array
description: |-
  Client versions that are available in the product.
  
x-akamai:
  file-path: schemas/client-versions.yaml
minItems: 1
items:
  type: object
  additionalProperties: false
  properties:
    name:
      type: string
      description: |
        Client OS name.
    date:
      type: string
      description: |
        Date when this browser version became available.
    latest:
      type: string
      description: |
        The latest client version number.
    upToDate:
      type: array
      description: |
        Up to date version properties.
      items:
        type: object
        additionalProperties: false
        properties:
          version:
            type: string
            description: |
              Up to date version number.
            minLength: 1
          date:
            type: string
            description: |
              The release date of the up to date version number.
            minLength: 1
