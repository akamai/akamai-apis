type: object
description: |-
  URL location properties.
  
x-akamai:
  file-path: schemas/create-urllocation-body.yaml
additionalProperties: true
required:
- rule_type
- name
- url
properties:
  name:
    type: string
    description: |
      The name of the URL location rule.
  description:
    type: string
    description: |
      The URL location description.
  url:
    type: string
    description: |
      The URL to map the application to.
  rule_type:
    type: integer
    description: |
      The rule type. Refer to [Rule types & actions](https://techdocs.akamai.com/eaa-api/reference/rule-types-actions) for available values.
    enum:
    - 1
    - 2
    - 3
    - 4
    - 5
    - 6
