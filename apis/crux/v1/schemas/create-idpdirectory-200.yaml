type: array
x-akamai:
  file-path: schemas/create-idpdirectory-200.yaml
items:
  type: object
  additionalProperties: false
  properties:
    directory:
      type: string
      description: |
        The unique ID of the directory created.
    resource:
      type: string
      description: |
        The IDP directory resource name.
      minLength: 1
    resource_uri:
      type: object
      description: |
        Specifies the location of various IDP directory resources.
      additionalProperties: false
      required:
      - directory
      - href
      - idp
      properties:
        directory:
          type: string
          description: |
            The URL for the directory.
          minLength: 1
        groups:
          type: string
          description: |
            The URL for group details.
          minLength: 1
        href:
          type: string
          description: |
            The URL for IDP directory details.
          minLength: 1
        idp:
          type: string
          description: |
            The URL for the IDP.
          minLength: 1
        users:
          type: string
          description: |
            The URL for users.
          minLength: 1
    uuid_url:
      type: string
      description: |
        A unique identifier for the directory.
      minLength: 1
