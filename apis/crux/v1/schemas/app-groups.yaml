type: object
description: |-
  Application groups.
  
x-akamai:
  file-path: schemas/app-groups.yaml
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Application groups. 
    items:
      $ref: app-group.yaml
