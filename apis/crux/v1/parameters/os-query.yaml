name: os
in: query
description: |-
  Filter records by operating system. Either `mac`, `windows`, `ios`, or `android`.
  
x-akamai:
  file-path: parameters/os-query.yaml
schema:
  type: string
  example: mac
  enum:
  - mac
  - windows
  - ios
  - android
