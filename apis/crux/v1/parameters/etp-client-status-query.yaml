name: etpClientStatus
in: query
description: |-
  Filter records by ETP client installation status. Either `installed` or `not-installed`.
  
x-akamai:
  file-path: parameters/etp-client-status-query.yaml
schema:
  type: string
  example: not-installed
  enum:
  - installed
  - not-installed
