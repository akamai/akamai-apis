name: jailbroken
in: query
description: |-
  When enabled, the response returns only jailbroken devices.
  
x-akamai:
  file-path: parameters/jailbroken-query.yaml
schema:
  type: boolean
  example: true
