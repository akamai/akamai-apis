name: res_type
in: query
description: |-
  Name of the resource type. Either `user`, `application`, or `group`.
  
x-akamai:
  file-path: parameters/res-type-query.yaml
required: true
schema:
  type: string
  example: user
  enum:
  - user
  - application
  - group
