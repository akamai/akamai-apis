name: expand
in: query
description: |-
  When set to `true`, includes additional members in the request.
  
x-akamai:
  file-path: parameters/expand-query.yaml
schema:
  type: boolean
  example: true
