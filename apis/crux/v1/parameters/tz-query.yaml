name: tz
in: query
description: |-
  The [tz database](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones) timezone of the requested `start` and `end` parameters.
  
x-akamai:
  file-path: parameters/tz-query.yaml
required: true
schema:
  type: string
  example: Asia/Calcutta
