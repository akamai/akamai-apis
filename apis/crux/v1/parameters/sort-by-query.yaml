name: sortBy
in: query
description: |-
  Column name to sort the record set by. Either `deviceName`, `deviceId`, `userName`, `os`, `timestamp`, `idpUserName`, or `id`.
  
x-akamai:
  file-path: parameters/sort-by-query.yaml
schema:
  type: string
  example: timestamp
  enum:
  - deviceName
  - deviceId
  - userName
  - os
  - timestamp
  - id
