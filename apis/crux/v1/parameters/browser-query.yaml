name: browser
in: query
description: |-
  Filter records by browser. Either `edge`, `firefox`, `chrome`, or `safari`.
  
x-akamai:
  file-path: parameters/browser-query.yaml
schema:
  type: string
  example: chrome
  enum:
  - edge
  - firefox
  - chrome
  - safari
