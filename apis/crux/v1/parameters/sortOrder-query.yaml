name: sortOrder
in: query
description: |-
  The sort direction for the column. Either `ASC` or `DESC`.
  
x-akamai:
  file-path: parameters/sortOrder-query.yaml
schema:
  type: string
  example: DESC
  enum:
  - ASC
  - DESC
