name: firewall
in: query
description: |-
  Filter records by firewall status. Either `good` or `poor`.
  
x-akamai:
  file-path: parameters/firewall-query.yaml
schema:
  type: string
  example: good
  enum:
  - good
  - poor
