name: event_type
in: query
description: |-
  Name of the event type. Either `login`, `config`, or `system`.
  
x-akamai:
  file-path: parameters/event-type-query.yaml
required: true
schema:
  type: string
  example: login
  enum:
  - config
  - login
  - system
