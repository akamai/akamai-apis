description: |-
  An access key with the provided `accessUid` doesn't exist.
  
x-akamai:
  file-path: errors/404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/access-key-get-404.json
    schema:
      $ref: ../schemas/problem.yaml
