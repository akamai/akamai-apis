name: all
in: query
description: |-
  Whether to unsubscribe from all CVEs, `false` by default.
  
x-akamai:
  file-path: parameters/all-query.yaml
schema:
  type: boolean
  example: true
  default: false
