name: createNewResourcesOnly
in: query
description: |-
  This creates an onboarding with a new CPS certificate and security protections. If your contract limits how many certificates you create, the API responds with an error.
  
x-akamai:
  file-path: parameters/create-new-resources-only-path.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
