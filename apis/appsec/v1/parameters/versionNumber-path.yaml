name: versionNumber
in: path
description: |-
  A unique identifier for each version of a configuration.
  
x-akamai:
  file-path: parameters/versionNumber-path.yaml
required: true
schema:
  type: integer
  example: 25
