name: sortOrder
in: query
description: |-
  The order in which you want to sort the results. Use `ASC` to sort in ascending order. Use `DESC` to sort by descending order. The default sort order is descending.
  
x-akamai:
  file-path: parameters/sort-order-query.yaml
required: true
schema:
  type: string
  example: ASC
  default: DESC
  enum:
  - ASC
  - DESC
