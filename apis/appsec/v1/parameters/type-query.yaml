name: type
in: query
description: |-
  If `ACTIVE`, includes only active recommendations, the default. If `EVALUATION`, includes only evaluation recommendations. If `ALL`, includes both active and evaluation recommendations.
  
x-akamai:
  file-path: parameters/type-query.yaml
schema:
  type: string
  example: ACTIVE
  default: ACTIVE
  enum:
  - ACTIVE
  - EVALUATION
  - ALL
