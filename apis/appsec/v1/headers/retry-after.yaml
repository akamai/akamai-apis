description: |-
  The estimated number of seconds until the task completes. This header only appears for tasks still in progress.
  
x-akamai:
  file-path: headers/retry-after.yaml
required: false
schema:
  type: integer
  example: 60
