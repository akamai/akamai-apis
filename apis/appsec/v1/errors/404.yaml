description: |-
  [Not found](ref:404). That resource doesn't exist, or the URL is malformed.
  
x-akamai:
  file-path: errors/404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/404.json
    schema:
      $ref: ../schemas/problem-details.yaml
