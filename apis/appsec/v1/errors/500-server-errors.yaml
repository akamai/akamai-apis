description: |-
  [Internal server error](ref:500). Something went wrong on our side. Try again in a few minutes, and contact support if the error persists.
  
x-akamai:
  file-path: errors/500-server-errors.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/problem-details.json
    schema:
      $ref: ../schemas/problem-details.yaml
