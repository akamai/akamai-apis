type: object
description: |-
  Contains details about a custom deny action.
  
x-akamai:
  file-path: schemas/custom-deny-diff.yaml
additionalProperties: false
properties:
  id:
    type: object
    description: |
      __Read-only__ Uniquely identifies the custom deny action. (This object [represents a diff](doc:diff-configuration-versions)).
    readOnly: true
    properties:
      from:
        type: string
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: string
        description: |
          Value from the version assigned as `to`.
        nullable: true
  name:
    type: object
    description: |
      The name you assigned to the custom deny action. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: string
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: string
        description: |
          Value from the version assigned as `to`.
        nullable: true
  description:
    type: object
    description: |
      Describes the custom deny action. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: string
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: string
        description: |
          Value from the version assigned as `to`.
        nullable: true
  parameters:
    type: object
    description: |
      Describes the custom deny parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          type: object
          description: |
            Contains a list of parameters for the custom deny action. These parameters are not the same type of parameters you usually include in the path of a request.
          additionalProperties: false
          properties:
            name:
              type: object
              description: |
                The custom deny parameter you choose instead of the Akamai default response. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            value:
              type: object
              description: |
                The value you assign to the custom deny parameter. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            displayName:
              type: object
              description: |
                The description of the custom deny parameter. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            outcome:
              $ref: outcome-diff.yaml
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          description: |
            Contains a list of parameters for the custom deny action. These parameters are not the same type of parameters you usually include in the path of a request.
          additionalProperties: false
          properties:
            name:
              type: object
              description: |
                The custom deny parameter you choose instead of the Akamai default response. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            value:
              type: object
              description: |
                The value you assign to the custom deny parameter. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            displayName:
              type: object
              description: |
                The description of the custom deny parameter. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            outcome:
              $ref: outcome-diff.yaml
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          description: |
            Contains a list of parameters for the custom deny action. These parameters are not the same type of parameters you usually include in the path of a request.
          additionalProperties: false
          properties:
            name:
              type: object
              description: |
                The custom deny parameter you choose instead of the Akamai default response. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            value:
              type: object
              description: |
                The value you assign to the custom deny parameter. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            displayName:
              type: object
              description: |
                The description of the custom deny parameter. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            outcome:
              $ref: outcome-diff.yaml
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          description: |
            Contains a list of parameters for the custom deny action. These parameters are not the same type of parameters you usually include in the path of a request.
          additionalProperties: false
          properties:
            name:
              type: object
              description: |
                The custom deny parameter you choose instead of the Akamai default response. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            value:
              type: object
              description: |
                The value you assign to the custom deny parameter. For available values, see [Deny name values](ref:deny-name-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            displayName:
              type: object
              description: |
                The description of the custom deny parameter. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            outcome:
              $ref: outcome-diff.yaml
  changesCount:
    $ref: changes-count-diff.yaml
  outcome:
    $ref: outcome-diff.yaml
