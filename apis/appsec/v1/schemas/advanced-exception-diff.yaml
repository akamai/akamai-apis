type: object
description: |-
  Describes the advanced exception members that allow you to conditionally exclude requests from inspection. This is only available for attack groups and when the advanced exception feature is enabled.
  
x-akamai:
  file-path: schemas/advanced-exception-diff.yaml
additionalProperties: false
properties:
  changesCount:
    $ref: changes-count-diff.yaml
  conditionOperator:
    type: object
    description: |
      Use `OR` to match any condition, or `AND` to match on all conditions. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: string
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: string
        description: |
          Value from the version assigned as `to`.
        nullable: true
  conditions:
    type: object
    description: |
      The list of match conditions. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          $ref: condition-diff.yaml
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          $ref: condition-diff.yaml
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          $ref: condition-diff.yaml
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          $ref: condition-diff.yaml
  headerCookieOrParamValues:
    type: object
    description: |
      The list of excepted values in headers, cookies, or query parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            outcome:
              $ref: outcome-diff.yaml
            valueWildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            values:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            outcome:
              $ref: outcome-diff.yaml
            valueWildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            values:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            outcome:
              $ref: outcome-diff.yaml
            valueWildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            values:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            outcome:
              $ref: outcome-diff.yaml
            valueWildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            values:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
  outcome:
    $ref: outcome-diff.yaml
  specificHeaderCookieOrParamNameValue:
    type: object
    description: |
      Contains details about the excepted name-value pairs in a request. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            namesValues:
              type: object
              description: |
                A list of name-value pairs to except. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            namesValues:
              type: object
              description: |
                A list of name-value pairs to except. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            namesValues:
              type: object
              description: |
                A list of name-value pairs to except. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            namesValues:
              type: object
              description: |
                A list of name-value pairs to except. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      changesCount:
                        $ref: changes-count-diff.yaml
                      names:
                        type: object
                        description: |
                          The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      outcome:
                        $ref: outcome-diff.yaml
                      values:
                        type: object
                        description: |
                          The list of request attribute values. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
  specificHeaderCookieParamXmlOrJsonNames:
    type: object
    description: |
      "Encapsulates the original and changed values, \nDescribes the advanced exception members that allow you to conditionally
      exclude requests from inspection. This is only available for attack groups and
      when the advanced exception feature is enabled. (This object [represents a diff](doc:diff-configuration-versions)
      of array data.)\n"
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        minItems: 0
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            names:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            names:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            names:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          additionalProperties: false
          properties:
            changesCount:
              $ref: changes-count-diff.yaml
            criteria:
              type: object
              description: |
                A list of criteria to limit the scope of this exception. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    $ref: criteria-diff.yaml
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    $ref: criteria-diff.yaml
            names:
              type: object
              description: |
                The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: string
                from:
                  type: array
                  description: |
                    Value from the version assigned as `from`.
                  items:
                    type: string
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: string
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: string
                to:
                  type: array
                  description: |
                    Value from the version assigned as `to`.
                  items:
                    type: string
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: string
            outcome:
              $ref: outcome-diff.yaml
            selector:
              type: object
              description: |
                The request attribute to exclude from inspection. See [Exception selector values](ref:exception-selector-values). (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            wildcard:
              type: object
              description: |
                Whether the provided header `name` is a wildcard. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: boolean
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: boolean
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
