type: object
description: |-
  List the existing subscribers of a feature for a specific security configuration.
  
x-akamai:
  file-path: schemas/appsec-config-subscription-200.yaml
additionalProperties: false
required:
- emails
properties:
  emails:
    type: array
    description: |
      Existing subscriber.
    items:
      type: string
