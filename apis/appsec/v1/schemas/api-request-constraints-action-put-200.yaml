type: object
description: |-
  The PUT Response JSON for API Request Constraints action.
  
x-akamai:
  file-path: schemas/api-request-constraints-action-put-200.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The action for API Request Constraints, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
