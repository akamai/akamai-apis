type: object
description: |-
  Contains bypass network lists for the specified configuration version.
  
x-akamai:
  file-path: schemas/bypass-network-lists-put.yaml
additionalProperties: false
required:
- networkLists
properties:
  networkLists:
    type: array
    description: |
      Contains a list of network list IDs.
    items:
      type: string
      description: |
        List of network lists. The values are the network lists' IDs.
