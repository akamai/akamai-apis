type: object
description: |-
  The GET Response JSON for eval conditions and exceptions.
  
x-akamai:
  file-path: schemas/eval-condition-exception-get-200.yaml
additionalProperties: false
properties:
  conditions:
    type: array
    description: |
      The conditions list of a rule or attack group.
    items:
      $ref: condition.yaml
  exception:
    $ref: exception.yaml
