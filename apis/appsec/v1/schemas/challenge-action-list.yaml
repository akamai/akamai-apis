type: object
x-akamai:
  file-path: schemas/challenge-action-list.yaml
additionalProperties: false
required:
- challengeActions
- validation
properties:
  challengeActions:
    type: array
    description: |
      __Read-only__ The list of challenge actions.
    readOnly: true
    items:
      $ref: challenge-action.yaml
  validation:
    readOnly: true
    $ref: validations.yaml
