description: |-
  Contains details about APIs discovered by analyzing your existing traffic.
  
x-akamai:
  file-path: schemas/api-discovered-list.yaml
additionalProperties: false
required:
- apis
properties:
  apis:
    type: array
    description: |
      A list of APIs that have been discovered but are not yet protected by API Protections.
    items:
      $ref: api-discovery.yaml
