type: object
description: |-
  The GET Response JSON for reputation analysis settings.
  
x-akamai:
  file-path: schemas/reputation-analysis-settings-get-200.yaml
additionalProperties: false
properties:
  forwardSharedIPToHTTPHeaderAndSIEM:
    type: boolean
    description: |
      Turn on/off the option to add value indicating that shared IPs are included in HTTP header and SIEM integration when used.
  forwardToHTTPHeader:
    type: boolean
    description: |
      Turn on/off the option to add client reputation details to requests forwarded to origin in an HTTP header.
