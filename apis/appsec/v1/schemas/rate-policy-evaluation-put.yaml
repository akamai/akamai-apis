type: object
description: |-
  Lists actions you can perform on a rate policy evaluation.
  
x-akamai:
  file-path: schemas/rate-policy-evaluation-put.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      Specifies the action to perform on a rate policy evaluation, either `APPLY` or `DISCARD`.
    enum:
    - APPLY
    - DISCARD
