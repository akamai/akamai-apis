type: object
description: |-
  The PUT Request JSON for an attack group action.
  
x-akamai:
  file-path: schemas/attack-group-action-put.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The attack group action, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
