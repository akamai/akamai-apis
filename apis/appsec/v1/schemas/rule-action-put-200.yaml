type: object
description: |-
  The PUT Response JSON for setting a rule action.
  
x-akamai:
  file-path: schemas/rule-action-put-200.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The action of the rule, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
