type: object
description: |-
  The PUT Request JSON for setting a rule action.
  
x-akamai:
  file-path: schemas/rule-action-put.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The action of the rule, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
