type: string
description: |-
  The result of the difference between the two versions, either `ADDED`, `REMOVED`, `MODIFIED`, or `UNMODIFIED`.
  
x-akamai:
  file-path: schemas/outcome-diff.yaml
enum:
- ADDED
- REMOVED
- MODIFIED
- UNMODIFIED
