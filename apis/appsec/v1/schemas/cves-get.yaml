type: object
description: |-
  The set of known CVEs.
  
x-akamai:
  file-path: schemas/cves-get.yaml
additionalProperties: false
required:
- cves
properties:
  cves:
    type: array
    description: |
      Lists CVEs.
    minItems: 0
    items:
      $ref: cve-get.yaml
