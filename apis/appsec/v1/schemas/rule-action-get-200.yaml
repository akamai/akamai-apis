type: object
description: |-
  The GET Response JSON for a rule action.
  
x-akamai:
  file-path: schemas/rule-action-get-200.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The action of a rule, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
