x-akamai:
  file-path: schemas/export-definitions-diff.yaml
definitions:
  attackGroupActions:
    type: object
    description: |
      The list of attack group actions set in the security policy. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            action:
              type: object
              description: |
                The action to take when the risk score exceeds the set threshold. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            advancedExceptions:
              $ref: advanced-exception-diff.yaml
            changesCount:
              $ref: changes-count-diff.yaml
            exception:
              $ref: exception-diff.yaml
            group:
              type: string
              description: |
                The attack group name.
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the ruleset version.
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            action:
              type: object
              description: |
                The action to take when the risk score exceeds the set threshold. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            advancedExceptions:
              $ref: advanced-exception-diff.yaml
            changesCount:
              $ref: changes-count-diff.yaml
            exception:
              $ref: exception-diff.yaml
            group:
              type: string
              description: |
                The attack group name.
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the ruleset version.
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            action:
              type: object
              description: |
                The action to take when the risk score exceeds the set threshold. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            advancedExceptions:
              $ref: advanced-exception-diff.yaml
            changesCount:
              $ref: changes-count-diff.yaml
            exception:
              $ref: exception-diff.yaml
            group:
              type: string
              description: |
                The attack group name.
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the ruleset version.
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          additionalProperties: false
          properties:
            action:
              type: object
              description: |
                The action to take when the risk score exceeds the set threshold. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            advancedExceptions:
              $ref: advanced-exception-diff.yaml
            changesCount:
              $ref: changes-count-diff.yaml
            exception:
              $ref: exception-diff.yaml
            group:
              type: string
              description: |
                The attack group name.
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the ruleset version.
  ruleActions:
    type: object
    description: |
      The list of rules in the security policy and the actions to perform when a request violates the rule. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
    properties:
      added:
        type: array
        description: |
          Data added between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            id:
              type: integer
              description: |
                Uniquely identifies the rule.
            action:
              type: object
              description: |
                The action to take when a request violates the rule. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            conditions:
              type: object
              description: |
                The list of conditions that trigger the rule. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
            exceptions:
              type: object
              description: |
                Contains details about the exceptions under which the rule's action doesn't trigger.
              additionalProperties: false
              properties:
                anyHeaderCookieOrParam:
                  type: object
                  description: |
                    The list of request attributes to treat as rule exceptions. The possible values are `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, and `REQ_HDR` for a request header. Use this option if you can't get an exhaustive list of elements to exclude or the list is too large. You can exclude several attributes. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                changesCount:
                  $ref: changes-count-diff.yaml
                headerCookieOrParamValues:
                  type: object
                  description: |
                    The list of excepted values in headers, cookies, or query parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`.
                      items:
                        type: string
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`.
                      items:
                        type: string
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                outcome:
                  $ref: outcome-diff.yaml
                specificHeaderCookieOrParamNameValue:
                  type: object
                  description: |
                    Contains details about the excepted name-value pair in a request.
                  additionalProperties: false
                  properties:
                    name:
                      type: object
                      description: |
                        The name of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    value:
                      type: object
                      description: |
                        The value of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name-value pair. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamNames:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    names:
                      type: object
                      description: |
                        The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                      properties:
                        added:
                          type: array
                          description: |
                            Data added between the two versions.
                          items:
                            type: string
                        from:
                          type: array
                          description: |
                            Value from the version assigned as `from`.
                          items:
                            type: string
                        modified:
                          type: array
                          description: |
                            Data modified between the two versions.
                          items:
                            type: string
                        outcome:
                          $ref: outcome-diff.yaml
                        removed:
                          type: array
                          description: |
                            Data removed between the two versions.
                          items:
                            type: string
                        to:
                          type: array
                          description: |
                            Value from the version assigned as `to`.
                          items:
                            type: string
                        unmodified:
                          type: array
                          description: |
                            Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                          items:
                            type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamPrefix:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name prefix.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    prefix:
                      type: object
                      description: |
                        The request attribute name prefix. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name prefix. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the rule set version.
      modified:
        type: array
        description: |
          Data modified between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            id:
              type: integer
              description: |
                Uniquely identifies the rule.
            action:
              type: object
              description: |
                The action to take when a request violates the rule. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            conditions:
              type: object
              description: |
                The list of conditions that trigger the rule. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
            exceptions:
              type: object
              description: |
                Contains details about the exceptions under which the rule's action doesn't trigger.
              additionalProperties: false
              properties:
                anyHeaderCookieOrParam:
                  type: object
                  description: |
                    The list of request attributes to treat as rule exceptions. The possible values are `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, and `REQ_HDR` for a request header. Use this option if you can't get an exhaustive list of elements to exclude or the list is too large. You can exclude several attributes. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                changesCount:
                  $ref: changes-count-diff.yaml
                headerCookieOrParamValues:
                  type: object
                  description: |
                    The list of excepted values in headers, cookies, or query parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`.
                      items:
                        type: string
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`.
                      items:
                        type: string
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                outcome:
                  $ref: outcome-diff.yaml
                specificHeaderCookieOrParamNameValue:
                  type: object
                  description: |
                    Contains details about the excepted name-value pair in a request.
                  additionalProperties: false
                  properties:
                    name:
                      type: object
                      description: |
                        The name of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    value:
                      type: object
                      description: |
                        The value of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name-value pair. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamNames:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    names:
                      type: object
                      description: |
                        The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                      properties:
                        added:
                          type: array
                          description: |
                            Data added between the two versions.
                          items:
                            type: string
                        from:
                          type: array
                          description: |
                            Value from the version assigned as `from`.
                          items:
                            type: string
                        modified:
                          type: array
                          description: |
                            Data modified between the two versions.
                          items:
                            type: string
                        outcome:
                          $ref: outcome-diff.yaml
                        removed:
                          type: array
                          description: |
                            Data removed between the two versions.
                          items:
                            type: string
                        to:
                          type: array
                          description: |
                            Value from the version assigned as `to`.
                          items:
                            type: string
                        unmodified:
                          type: array
                          description: |
                            Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                          items:
                            type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamPrefix:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name prefix.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    prefix:
                      type: object
                      description: |
                        The request attribute name prefix. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name prefix. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the rule set version.
      outcome:
        $ref: outcome-diff.yaml
      removed:
        type: array
        description: |
          Data removed between the two versions.
        items:
          type: object
          additionalProperties: false
          properties:
            id:
              type: integer
              description: |
                Uniquely identifies the rule.
            action:
              type: object
              description: |
                The action to take when a request violates the rule. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            conditions:
              type: object
              description: |
                The list of conditions that trigger the rule. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
            exceptions:
              type: object
              description: |
                Contains details about the exceptions under which the rule's action doesn't trigger.
              additionalProperties: false
              properties:
                anyHeaderCookieOrParam:
                  type: object
                  description: |
                    The list of request attributes to treat as rule exceptions. The possible values are `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, and `REQ_HDR` for a request header. Use this option if you can't get an exhaustive list of elements to exclude or the list is too large. You can exclude several attributes. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                changesCount:
                  $ref: changes-count-diff.yaml
                headerCookieOrParamValues:
                  type: object
                  description: |
                    The list of excepted values in headers, cookies, or query parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`.
                      items:
                        type: string
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`.
                      items:
                        type: string
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                outcome:
                  $ref: outcome-diff.yaml
                specificHeaderCookieOrParamNameValue:
                  type: object
                  description: |
                    Contains details about the excepted name-value pair in a request.
                  additionalProperties: false
                  properties:
                    name:
                      type: object
                      description: |
                        The name of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    value:
                      type: object
                      description: |
                        The value of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name-value pair. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamNames:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    names:
                      type: object
                      description: |
                        The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                      properties:
                        added:
                          type: array
                          description: |
                            Data added between the two versions.
                          items:
                            type: string
                        from:
                          type: array
                          description: |
                            Value from the version assigned as `from`.
                          items:
                            type: string
                        modified:
                          type: array
                          description: |
                            Data modified between the two versions.
                          items:
                            type: string
                        outcome:
                          $ref: outcome-diff.yaml
                        removed:
                          type: array
                          description: |
                            Data removed between the two versions.
                          items:
                            type: string
                        to:
                          type: array
                          description: |
                            Value from the version assigned as `to`.
                          items:
                            type: string
                        unmodified:
                          type: array
                          description: |
                            Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                          items:
                            type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamPrefix:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name prefix.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    prefix:
                      type: object
                      description: |
                        The request attribute name prefix. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name prefix. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the rule set version.
      unmodified:
        type: array
        description: |
          Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
        items:
          type: object
          additionalProperties: false
          properties:
            id:
              type: integer
              description: |
                Uniquely identifies the rule.
            action:
              type: object
              description: |
                The action to take when a request violates the rule. (This object [represents a diff](doc:diff-configuration-versions)).
              properties:
                from:
                  type: string
                  description: |
                    Value from the version assigned as `from`.
                  nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                to:
                  type: string
                  description: |
                    Value from the version assigned as `to`.
                  nullable: true
            changesCount:
              $ref: changes-count-diff.yaml
            conditions:
              type: object
              description: |
                The list of conditions that trigger the rule. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
              properties:
                added:
                  type: array
                  description: |
                    Data added between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                modified:
                  type: array
                  description: |
                    Data modified between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                outcome:
                  $ref: outcome-diff.yaml
                removed:
                  type: array
                  description: |
                    Data removed between the two versions.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                unmodified:
                  type: array
                  description: |
                    Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      name:
                        type: object
                        description: |
                          The query parameter name that triggers the condition. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      type:
                        type: object
                        description: |
                          The condition type to match on. See [Export condition type values](ref:etval). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      paths:
                        type: object
                        description: |
                          The paths that trigger the condition. This only applies to the  `pathMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      value:
                        type: object
                        description: |
                          The query parameter value if the condition `type` is `uriQueryMatch` and header value if the condition `type` is `requestHeaderMatch`. This only applies to the `uriQueryMatch` and `requestHeaderMatch` condition `type`s. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      caseSensitive:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `value`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      changesCount:
                        $ref: changes-count-diff.yaml
                      extensions:
                        type: object
                        description: |
                          The file extensions that trigger the condition. This only applies to the `extensionMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      filenames:
                        type: object
                        description: |
                          The filenames that trigger the condition. This only applies to the `filenameMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      header:
                        type: object
                        description: |
                          The HTTP header that triggers the condition. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: string
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: string
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      hosts:
                        type: object
                        description: |
                          The hostnames that trigger the condition. This only applies to the `hostMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      ips:
                        type: object
                        description: |
                          The IPs that trigger the condition. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`.
                            items:
                              type: string
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`.
                            items:
                              type: string
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                      methods:
                        type: object
                        description: |
                          The HTTP request methods that trigger the condition. The possible values are `GET`, `POST`, `HEAD`, `PUT`, `DELETE`, `OPTIONS`, `TRACE`, `CONNECT`, and `PATCH`. This only applies to the `requestMethodMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                        properties:
                          added:
                            type: array
                            description: |
                              Data added between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          from:
                            type: array
                            description: |
                              Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          modified:
                            type: array
                            description: |
                              Data modified between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          outcome:
                            $ref: outcome-diff.yaml
                          removed:
                            type: array
                            description: |
                              Data removed between the two versions.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          to:
                            type: array
                            description: |
                              Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                          unmodified:
                            type: array
                            description: |
                              Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                            items:
                              type: string
                              enum:
                              - GET
                              - POST
                              - HEAD
                              - PUT
                              - DELETE
                              - OPTIONS
                              - TRACE
                              - CONNECT
                              - PATCH
                      nameCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided query parameter `name`. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      outcome:
                        $ref: outcome-diff.yaml
                      positiveMatch:
                        type: object
                        description: |
                          Whether the condition should trigger on a match (`true`) or a lack of match (`false`). (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      useHeaders:
                        type: object
                        description: |
                          Whether the condition should include the `X-Forwarded-For` (XFF) headers. This only applies to the `ipMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueCase:
                        type: object
                        description: |
                          Whether to consider the case-sensitivity of the provided header `value`. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      valueWildcard:
                        type: object
                        description: |
                          Whether the provided header `value` is a wildcard. This only applies to the `requestHeaderMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
                      wildcard:
                        type: object
                        description: |
                          Whether the provided query parameter `value` is a wildcard. This only applies to the `uriQueryMatch` condition `type`. (This object [represents a diff](doc:diff-configuration-versions)).
                        properties:
                          from:
                            type: boolean
                            description: |
                              Value from the version assigned as `from`.
                            nullable: true
                          outcome:
                            $ref: outcome-diff.yaml
                          to:
                            type: boolean
                            description: |
                              Value from the version assigned as `to`.
                            nullable: true
            exceptions:
              type: object
              description: |
                Contains details about the exceptions under which the rule's action doesn't trigger.
              additionalProperties: false
              properties:
                anyHeaderCookieOrParam:
                  type: object
                  description: |
                    The list of request attributes to treat as rule exceptions. The possible values are `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, and `REQ_HDR` for a request header. Use this option if you can't get an exhaustive list of elements to exclude or the list is too large. You can exclude several attributes. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`. (These represent the [compared data's enum values](doc:diff-configuration-versions).)
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                        enum:
                        - COOKIE
                        - JSON_PARAM
                        - XML_PARAM
                        - REQ_PARAM
                        - REQ_HDR
                changesCount:
                  $ref: changes-count-diff.yaml
                headerCookieOrParamValues:
                  type: object
                  description: |
                    The list of excepted values in headers, cookies, or query parameters. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                  properties:
                    added:
                      type: array
                      description: |
                        Data added between the two versions.
                      items:
                        type: string
                    from:
                      type: array
                      description: |
                        Value from the version assigned as `from`.
                      items:
                        type: string
                    modified:
                      type: array
                      description: |
                        Data modified between the two versions.
                      items:
                        type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    removed:
                      type: array
                      description: |
                        Data removed between the two versions.
                      items:
                        type: string
                    to:
                      type: array
                      description: |
                        Value from the version assigned as `to`.
                      items:
                        type: string
                    unmodified:
                      type: array
                      description: |
                        Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                      items:
                        type: string
                outcome:
                  $ref: outcome-diff.yaml
                specificHeaderCookieOrParamNameValue:
                  type: object
                  description: |
                    Contains details about the excepted name-value pair in a request.
                  additionalProperties: false
                  properties:
                    name:
                      type: object
                      description: |
                        The name of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    value:
                      type: object
                      description: |
                        The value of the request attribute. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name-value pair. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamNames:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    names:
                      type: object
                      description: |
                        The list of request attribute names. (This object [represents a diff](doc:diff-configuration-versions) of array data.)
                      properties:
                        added:
                          type: array
                          description: |
                            Data added between the two versions.
                          items:
                            type: string
                        from:
                          type: array
                          description: |
                            Value from the version assigned as `from`.
                          items:
                            type: string
                        modified:
                          type: array
                          description: |
                            Data modified between the two versions.
                          items:
                            type: string
                        outcome:
                          $ref: outcome-diff.yaml
                        removed:
                          type: array
                          description: |
                            Data removed between the two versions.
                          items:
                            type: string
                        to:
                          type: array
                          description: |
                            Value from the version assigned as `to`.
                          items:
                            type: string
                        unmodified:
                          type: array
                          description: |
                            Data that doesn't change between the two versions. This appears only when the diff request specifies to include `UNMODIFIED` data.
                          items:
                            type: string
                    outcome:
                      $ref: outcome-diff.yaml
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                specificHeaderCookieOrParamPrefix:
                  type: object
                  description: |
                    Contains details about the excepted request attribute name prefix.
                  additionalProperties: false
                  properties:
                    changesCount:
                      $ref: changes-count-diff.yaml
                    outcome:
                      $ref: outcome-diff.yaml
                    prefix:
                      type: object
                      description: |
                        The request attribute name prefix. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
                    selector:
                      type: object
                      description: |
                        The request attribute that includes the excepted name prefix. Either `COOKIE`, `JSON_PARAM` for a JSON parameter, `XML_PARAM` for an XML parameter, `REQ_PARAM` for a request parameter, or `REQ_HDR` for a request header. (This object [represents a diff](doc:diff-configuration-versions)).
                      properties:
                        from:
                          type: string
                          description: |
                            Value from the version assigned as `from`.
                          nullable: true
                        outcome:
                          $ref: outcome-diff.yaml
                        to:
                          type: string
                          description: |
                            Value from the version assigned as `to`.
                          nullable: true
            outcome:
              $ref: outcome-diff.yaml
            rulesetVersionId:
              type: integer
              description: |
                Uniquely identifies the rule set version.
