type: object
description: |-
  Take action on requests from Ukraine.
  
x-akamai:
  file-path: schemas/ukraine-geo-control.yaml
additionalProperties: false
properties:
  action:
    type: string
    description: |
      The action to take on requests from the Donetsk and Luhansk regions of Ukraine — either `alert`, `deny`, or `none`. Because this setting is intended to address fluid situations, Akamai will automatically update the included regions as necessary.
    enum:
    - alert
    - deny
    - none
