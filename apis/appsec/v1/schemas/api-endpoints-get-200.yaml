type: object
description: |-
  Specifies each API's basic metadata and current deployment status.
  
x-akamai:
  file-path: schemas/api-endpoints-get-200.yaml
additionalProperties: false
required:
- apiEndpoints
properties:
  apiEndpoints:
    type: array
    description: |
      The list of API Endpoints.
    items:
      $ref: api-endpoint.yaml
