type: object
description: |-
  The PUT Response JSON for an attack group action.
  
x-akamai:
  file-path: schemas/attack-group-action-put-200.yaml
additionalProperties: false
required:
- action
properties:
  action:
    type: string
    description: |
      The attack group action, either `alert`, `deny`, or `none`.
    enum:
    - alert
    - deny
    - none
