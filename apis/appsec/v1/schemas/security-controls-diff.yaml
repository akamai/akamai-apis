type: object
description: |-
  Describes the operational status of security controls.
  
x-akamai:
  file-path: schemas/security-controls-diff.yaml
additionalProperties: false
properties:
  applyApiConstraints:
    type: object
    description: |
      Whether you enabled API constraints. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applyApplicationLayerControls:
    type: object
    description: |
      Whether you enabled application layer controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applyBotmanControls:
    type: object
    description: |
      Whether you enabled Bot Manager controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applyNetworkLayerControls:
    type: object
    description: |
      Whether you enabled network layer controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applyRateControls:
    type: object
    description: |
      Whether you enabled rate controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applyReputationControls:
    type: object
    description: |
      Whether you enabled reputation controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  applySlowPostControls:
    type: object
    description: |
      Whether you enabled slow post controls. (This object [represents a diff](doc:diff-configuration-versions)).
    properties:
      from:
        type: boolean
        description: |
          Value from the version assigned as `from`.
        nullable: true
      outcome:
        $ref: outcome-diff.yaml
      to:
        type: boolean
        description: |
          Value from the version assigned as `to`.
        nullable: true
  changesCount:
    $ref: changes-count-diff.yaml
  outcome:
    $ref: outcome-diff.yaml
