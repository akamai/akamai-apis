description: |-
  No data is available for the specified domain, resource, and datacenter.
  
x-akamai:
  file-path: errors/404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/404.json
    schema:
      $ref: ../schemas/problem.yaml
