description: |-
  The user specified has no entitlement to access data for the domain, resource, and datacenter.
  
x-akamai:
  file-path: errors/403.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/403.json
    schema:
      $ref: ../schemas/problem.yaml
