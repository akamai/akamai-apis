name: includeExpired
in: query
description: |-
  Whether to include expired tokens that are less than 30 days old in the response.
  
x-akamai:
  file-path: parameters/includeExpired-query.yaml
schema:
  type: boolean
  example: true
