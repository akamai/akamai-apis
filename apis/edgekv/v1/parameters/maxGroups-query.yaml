name: maxGroups
in: query
description: |-
  If specified, maximum number of groups to return per request up to the system [limits](doc:limits).
  
x-akamai:
  file-path: parameters/maxGroups-query.yaml
schema:
  type: integer
  example: 10
