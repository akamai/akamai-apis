name: maxItems
in: query
description: |-
  If specified, maximum number of items to return per request, up to the system [limits](doc:limits).
  
x-akamai:
  file-path: parameters/maxItems-query.yaml
schema:
  type: integer
  example: 10
