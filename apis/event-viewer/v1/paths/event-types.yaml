x-akamai:
  path-info: /event-types
get:
  operationId: get-event-types
  summary: List event types
  tags:
  - Event types
  description: |
    Returns all defined event types with related event definitions.
  responses:
    200:
      description: |
        List Event Types.
      content:
        application/json:
          example:
            $ref: ../examples/event-types-200.json
          schema:
            $ref: ../schemas/event-types-200.yaml
