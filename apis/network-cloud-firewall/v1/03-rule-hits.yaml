openapi: 3.0.0
info:
  title: ACL rule hits
  version: v1
servers:
- url: https://{hostname}/network-cloud-firewall/v1
tags:
- name: ACL rule hits
  description: |
    View ACL rule match statistics.
x-akamai:
  auth-type: EDGE_GRID
paths:
  /rule-hits/rules:
    $ref: paths/rule-hits.yaml
  /rule-hits/rules/{ruleId}:
    $ref: paths/rule-hits-by-id.yaml
  /rule-hits/rules/{ruleId}/time-series:
    $ref: paths/rule-hits-by-id-timeseries.yaml
