type: object
description: |
  ACL version state update properties.
additionalProperties: false
properties:
  state:
    type: string
    description: |
      ACL version state. Either `DEPLOYED` or `DEPRECATED`. Only ACL versions in `CONFIGURATION` state can be updated.
    enum:
    - DEPLOYED
    - DEPRECATED
