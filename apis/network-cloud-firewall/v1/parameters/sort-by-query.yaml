name: sortBy
in: query
description: |
  ACL version content property to sort by. Either `accountId`, `version`, `description`, `state`, `rulesCount`, `createdBy`, `updatedBy`, `createdTimestamp`, or `updatedTimestamp`.
required: false
schema:
  type: string
  example: version
  enum:
  - accountId
  - version
  - description
  - state
  - rulesCount
  - createdBy
  - updatedBy
  - createdTimestamp
  - updatedTimestamp
