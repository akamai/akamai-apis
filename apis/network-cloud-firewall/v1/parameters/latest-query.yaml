name: latest
in: query
description: |
  Returns the latest ACL versions for the account. If `true`, displays only ACL versions in `CONFIGURATION` and `DEPLOYED` states. If `false`, lists ACL versions in all states.
required: false
schema:
  type: boolean
  example: true
  default: false
