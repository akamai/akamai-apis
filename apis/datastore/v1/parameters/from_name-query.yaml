name: from_name
in: query
description: |-
  Returns messages with keys greater or equal to `from_name` from all available messages in alphabetical order.
  
x-akamai:
  file-path: parameters/from_name-query.yaml
schema:
  type: string
  example: AAX
  minLength: 1
  maxLength: 64
