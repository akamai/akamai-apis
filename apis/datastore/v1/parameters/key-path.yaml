name: key
in: path
description: |-
  The unique key value for an object stored in the data set. It can be up to 64 characters long.
  
x-akamai:
  file-path: parameters/key-path.yaml
required: true
schema:
  type: string
  example: AAX.dat
