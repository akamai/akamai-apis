name: Range
in: header
description: |-
  Indicates the part of a document that the server should return. Only single range is supported.
  
x-akamai:
  file-path: parameters/Range-header.yaml
schema:
  type: string
  example: bytes=0-499
