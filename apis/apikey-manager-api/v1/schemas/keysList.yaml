type: object
description: |-
  Contains information about the keys to reset the quota for.
  
x-akamai:
  file-path: schemas/keysList.yaml
additionalProperties: false
required:
- keys
properties:
  keys:
    type: array
    description: |
      The unique identifiers for the keys.
    minItems: 0
    items:
      type: integer
      minimum: 0
