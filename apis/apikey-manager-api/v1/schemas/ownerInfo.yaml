type: object
x-akamai:
  file-path: schemas/ownerInfo.yaml
additionalProperties: false
properties:
  accountId:
    type: string
    description: |
      A unique identifier for an account.
    minLength: 1
  contractTypeId:
    type: string
    description: |
      A unique identifier for a contract type.
    minLength: 1
  identity:
    type: string
    description: |
      A unique identifier for a user.
    minLength: 1
