type: object
description: |-
  Contains information about keys to revoke or restore.
  
x-akamai:
  file-path: schemas/revokeKeysCommand.yaml
additionalProperties: false
required:
- keys
properties:
  keys:
    type: array
    description: |
      The unique identifiers for the keys to revoke or restore.
    minItems: 1
    items:
      type: integer
      minimum: 0
