name: cpcodeName
in: query
description: |-
  The name of the CP code to filter data by. Include this optional parameter to narrow down the results to a specific CP code name only.
  
x-akamai:
  file-path: parameters/cpcodeName-query.yaml
schema:
  type: string
  example: CPC XYZ
