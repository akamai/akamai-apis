x-akamai:
  path-info: /zones/edit-secondary-zones
post:
  operationId: post-zones-edit-secondary-zones
  summary: Bulk edit secondary zones
  tags:
  - Zones
  description: |
    Updates settings for one or more secondary zones in a single operation.
  requestBody:
    required: true
    content:
      application/json:
        example:
          $ref: ../examples/edit-secondary-zones-post-request.json
        schema:
          $ref: ../schemas/bulk-secondary-zone-edit-request.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/edit-secondary-zones-post-response.json
          schema:
            $ref: ../schemas/bulk-secondary-zone-edit-response.yaml
