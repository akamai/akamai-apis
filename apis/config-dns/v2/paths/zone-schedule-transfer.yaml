x-akamai:
  path-info: /zones/{zone}/schedule-immediate-transfer
parameters:
- $ref: ../parameters/zone-path.yaml
put:
  operationId: put-zones-zone-schedule-immediate-transfer
  summary: Schedule an immediate zone transfer
  tags:
  - Zones
  description: |
    Schedules an immediate zone transfer for the specified secondary zone, bypassing any pending transfer delays.
  responses:
    200:
      description: |
        Zone transfer scheduled successfully.
