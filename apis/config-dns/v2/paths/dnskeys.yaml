x-akamai:
  path-info: /zones/{zone}/dnskeys
parameters:
- $ref: ../parameters/zone-path.yaml
get:
  operationId: get-zone-dnskeys
  summary: Get a zone's DNSSEC DNSKEY records
  tags:
  - DNSSEC keys
  description: |
    Get the current DNSSEC DNSKEY records for a zone.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/dnskeys-get.json
          schema:
            $ref: ../schemas/recordset.yaml
