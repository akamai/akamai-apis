name: isExpired
in: query
description: |-
  Returns requests based on request expiration.
  
x-akamai:
  file-path: parameters/is-expired-query.yaml
schema:
  type: boolean
  example: true
  default: false
