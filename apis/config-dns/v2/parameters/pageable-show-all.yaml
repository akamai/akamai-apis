name: showAll
in: query
description: |-
  Disables paging and sorting and shows the entire list all at once, `false` by default. Using this parameter may significantly increase the response time.
  
x-akamai:
  file-path: parameters/pageable-show-all.yaml
schema:
  type: boolean
  example: true
  default: false
