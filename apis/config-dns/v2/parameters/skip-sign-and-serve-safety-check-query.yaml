name: skipSignAndServeSafetyCheck
in: query
description: |-
  If set to true, skips safety checks when disabling sign and serve.
  
x-akamai:
  file-path: parameters/skip-sign-and-serve-safety-check-query.yaml
required: false
schema:
  type: boolean
  example: false
