name: subzoneGrants
in: query
description: |
  If included, limits the Edge DNS zones where `subzoneGrants` matches the specified boolean. Refer to [Subzone grants](ref:subzone-grants) for more information.
schema:
  type: boolean
  default: false
  example: true
