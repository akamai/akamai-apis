name: skipSignAndServeSafetyCheck
in: query
description: |-
  If set to true, skips safety checks when disabling sign and serve.
  
x-akamai:
  file-path: parameters/skipSignAndServeSafetyCheck-query.yaml
required: true
schema:
  type: boolean
  example: false
