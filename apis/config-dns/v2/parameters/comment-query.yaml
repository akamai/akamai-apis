name: comment
in: query
description: |-
  A new comment string that may be blank. If not included, the old comment is copied.
  
x-akamai:
  file-path: parameters/comment-query.yaml
schema:
  type: string
  example: Updating A record
