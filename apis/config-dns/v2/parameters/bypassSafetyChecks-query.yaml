name: bypassSafetyChecks
in: query
description: |-
  If `true`, disables the delegation checks and deletes the zones as soon as possible.
  
x-akamai:
  file-path: parameters/bypassSafetyChecks-query.yaml
schema:
  type: boolean
  example: false
  default: false
