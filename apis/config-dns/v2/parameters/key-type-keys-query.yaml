name: keyType
in: query
description: |
  Limits the response to zones of the specified TSIG key type. Either `inbound`, `outbound`, or `proxy`.
schema:
  type: string
  enum:
  - inbound
  - outbound
  - proxy
  example: inbound
