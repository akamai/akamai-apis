name: keyType
in: query
description: |-
  Limits the response to zones of the specified TSIG key type. Either `inbound`, `outbound`, or `proxy`.
  
x-akamai:
  file-path: parameters/key-type-keys-query.yaml
schema:
  type: string
  example: inbound
  enum:
  - inbound
  - outbound
  - proxy
