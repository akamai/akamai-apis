name: pageSize
in: query
description: |-
  The number of results per page to return, `25` by default.
  
x-akamai:
  file-path: parameters/pageable-page-size.yaml
schema:
  type: integer
  example: 25
  default: 25
