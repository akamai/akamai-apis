name: showAll
in: query
description: |-
  Disables paging and shows the entire list all at once. Using this parameter may significantly increase the time required to respond to the request.
  
x-akamai:
  file-path: parameters/pageable_showAll.yaml
schema:
  type: boolean
  example: false
  default: false
