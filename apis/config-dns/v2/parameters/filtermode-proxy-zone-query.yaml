name: filterMode
in: query
description: |-
  Limits the proxy zones belonging to the specified filter modes.
  
x-akamai:
  file-path: parameters/filtermode-proxy-zone-query.yaml
schema:
  type: string
  example: AUTOMATIC,MANUAL
