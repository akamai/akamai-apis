name: filterMode
in: query
description: |
  Limits the proxy zones belonging to the specified filter modes. Specify any combination of `AUTOMATIC` and `MANUAL` values.
schema:
  type: array
  items:
    type: string
    enum:
    - AUTOMATIC
    - MANUAL
  example: [AUTOMATIC]
explode: false
style: form
