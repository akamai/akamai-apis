name: search
in: query
description: |-
  Display only record sets whose RDATA field matches this value. The entire record set, that is all records with the same name and type, displays if any record in the set has RDATA that matches the supplied value.
  
x-akamai:
  file-path: parameters/search-query-rdata.yaml
schema:
  type: string
  example: www
