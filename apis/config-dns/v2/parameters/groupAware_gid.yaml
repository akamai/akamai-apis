name: gid
in: query
description: |-
  The currently selected group ID. This is a feature in the Pulsar UI where a user is able to specify a particular group to manage.
  
x-akamai:
  file-path: parameters/groupAware_gid.yaml
required: true
schema:
  type: integer
  example: 100
