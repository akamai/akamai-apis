name: search
in: query
description: |
  Display only record sets whose RDATA field matches this value. All records with the same name and type display if any record in the set has RDATA that matches the supplied value.
schema:
  type: string
  example: 192.0.2.1
