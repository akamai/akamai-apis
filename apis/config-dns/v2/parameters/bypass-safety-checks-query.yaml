name: bypassSafetyChecks
in: query
description: |-
  If `true`, disables the delegation checks and deletes the zones as soon as possible. It's `false` by default.
  
x-akamai:
  file-path: parameters/bypass-safety-checks-query.yaml
schema:
  type: boolean
  example: true
  default: false
