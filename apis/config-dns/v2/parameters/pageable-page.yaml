name: page
in: query
description: |-
  Which page of results to return. The first page is page `1`, the default value.
  
x-akamai:
  file-path: parameters/pageable-page.yaml
schema:
  type: integer
  example: 1
  default: 1
