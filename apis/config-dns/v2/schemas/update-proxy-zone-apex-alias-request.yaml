type: object
description: |
  An Edge DNS proxy zones apex alias record.
additionalProperties: false
required:
- apexAlias
properties:
  apexAlias:
    type: string
    description: |
      An Edge DNS proxy zones apex alias record.
