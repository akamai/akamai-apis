type: object
description: |-
  An Edge DNS proxy zones apex alias record.
  
x-akamai:
  file-path: schemas/update-proxy-zone-apex-alias-request.yaml
additionalProperties: false
required:
- apexAlias
properties:
  apexAlias:
    type: string
    description: |
      An Edge DNS proxy zones apex alias record.
