type: object
additionalProperties: false
required:
- contracts
properties:
  contracts:
    type: array
    description: |
      A customer contract and associated permissions.
    items:
      $ref: contract-get.yaml
