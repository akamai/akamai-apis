type: object
description: |-
  A list of record set types that can be used with a particular zone.
  
x-akamai:
  file-path: schemas/recordtype-list.yaml
additionalProperties: false
required:
- types
properties:
  types:
    type: array
    items:
      type: string
