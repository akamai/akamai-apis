type: object
description: |
  Record set types that can be used with a particular zone.
additionalProperties: false
required:
- types
properties:
  types:
    type: array
    description: |
      Record set types that can be used with a particular zone.
    items:
      type: string
