type: object
description: |
  Record names.
additionalProperties: false
required:
- names
properties:
  names:
    type: array
    description: |
      Record names.
    uniqueItems: true
    items:
      type: string
