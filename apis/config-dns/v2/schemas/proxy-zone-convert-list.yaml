type: object
description: |
  Proxy zone convert data.
additionalProperties: false
required:
- proxyZones
properties:
  proxyZones:
    type: array
    description: |
      Proxy zone names.
    items:
      type: string
  tsigKey:
    $ref: tsigkey-post.yaml
  manualFilterNames:
    type: array
    description: |
      Manual filter names.
    items:
      type: string
