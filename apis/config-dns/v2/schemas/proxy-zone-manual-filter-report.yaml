type: object
description: |
  An Edge DNS proxy zone manual config report.
additionalProperties: false
required:
- metadata
- manualFilterNames
properties:
  metadata:
    type: object
    description: |
      Information about the request.
  manualFilterNames:
    type: array
    description: |
      Proxy zone manual config entities.
    items:
      type: string
