additionalProperties: false
type: object
required:
- matchingKeyTags
properties:
  matchingKeyTags:
    description: List of key tag values from DS records that match the zone's current DNSKEY records.
    type: array
    items:
      type: integer
