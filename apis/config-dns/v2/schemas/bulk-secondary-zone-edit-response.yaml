additionalProperties: false
type: object
required:
- zones
properties:
  zones:
    description: List of zones successfully updated, each with its new version ID.
    type: array
    items:
      additionalProperties: false
      type: object
      required:
      - zone
      - versionId
      properties:
        zone:
          description: The zone name.
          type: string
        versionId:
          description: The new `ETag` version identifier for the zone after the update.
          type: string
