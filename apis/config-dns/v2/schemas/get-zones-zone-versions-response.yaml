type: object
additionalProperties: false
required:
- metadata
- versions
properties:
  metadata:
    type: object
    description: |
      Information about the request.
  versions:
    type: array
    description: |
      Edge DNS zones. Each object contains zone settings, but not record sets.
    items:
      $ref: zone-get.yaml
