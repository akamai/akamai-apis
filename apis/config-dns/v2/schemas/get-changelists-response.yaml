type: object
additionalProperties: false
required:
- changeLists
properties:
  changeLists:
    type: array
    description: |
      Metadata about a change list. Includes the zone version that the change list was derived from when it was created.
    items:
      $ref: changelist-get.yaml
