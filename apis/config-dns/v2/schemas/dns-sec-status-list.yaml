type: object
description: |
  Statuses of DNSSEC configurations for Edge DNS zones.
additionalProperties: false
required:
- dnsSecStatuses
properties:
  dnsSecStatuses:
    type: array
    description: |
      Statuses of the DNSSEC configuration for Edge DNS zones.
    items:
      $ref: dns-sec-status.yaml
