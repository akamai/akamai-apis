type: object
description: |-
  A list of statuses of DNSSEC configurations for Edge DNS zones.
  
x-akamai:
  file-path: schemas/dns-sec-status-list.yaml
additionalProperties: false
required:
- dnsSecStatuses
properties:
  dnsSecStatuses:
    type: array
    description: |
      The list of statuses of the DNSSEC configuration for Edge DNS zones.
    items:
      $ref: dns-sec-status.yaml
