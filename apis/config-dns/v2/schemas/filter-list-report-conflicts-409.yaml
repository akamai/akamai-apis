type: object
description: |-
  An Edge DNS proxy zone manual config list report conflict error.
  
x-akamai:
  file-path: schemas/filter-list-report-conflicts-409.yaml
additionalProperties: true
properties:
  type:
    type: string
    description: |
      A non-navigable URI that identifies each error case.
    format: uri
  title:
    type: string
    description: |
      Error type name.
  detail:
    type: string
    description: |
      Detailed description of the error.
  instance:
    type: string
    description: |
      Error instance.
  manualConfigName:
    type: string
    description: |
      The conflicting manual configuration name.
  problemId:
    type: string
    description: |
      Problem instance ID.
  status:
    type: integer
    description: |
      The HTTP error status code.
