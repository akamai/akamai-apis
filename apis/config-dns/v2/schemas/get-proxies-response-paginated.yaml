type: object
additionalProperties: false
required:
- proxyZones
properties:
  metadata:
    type: object
    description: |
      Information about the request.
  proxyZones:
    type: array
    description: |
      Proxy zones.
    items:
      $ref: proxy-zone-post.yaml
