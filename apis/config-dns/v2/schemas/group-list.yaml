type: object
additionalProperties: false
required:
- groups
properties:
  groups:
    type: array
    description: |
      A customer group and associated permissions.
    items:
      $ref: group-get.yaml
