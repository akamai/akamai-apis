type: object
description: |
  SoaSerialSummaryResponse for secondary zones.
additionalProperties: false
required:
- soaSerialLocks
properties:
  soaSerialLocks:
    type: array
    description: |
     The SOA serial number for the zone. This value must be present in the SOA record retrieved from the user.
    items:
      $ref: zone-convert-primary-post.yaml
