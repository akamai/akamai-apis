type: object
description: |
  A request to list of SOA serials for secondary zones.
additionalProperties: false
required:
- zones
properties:
  zones:
    type: array
    description: |
      zone names
    items:
      type: string