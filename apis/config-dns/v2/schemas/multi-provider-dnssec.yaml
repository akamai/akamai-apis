type: object
description: |
  Multi-provider DNSSEC properties.
additionalProperties: false
required:
- enabled
properties:
  enabled:
    type: boolean
    description: |
      Enables use of multi-provider DNSSEC functionality.
  webhook:
    type: string
    description: |
      Customer URL to call when a new ZSK secret has been generated.
