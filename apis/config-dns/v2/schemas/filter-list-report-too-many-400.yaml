type: object
description: |-
  An Edge DNS proxy zone manual config list report limit error.
  
x-akamai:
  file-path: schemas/filter-list-report-too-many-400.yaml
additionalProperties: true
properties:
  type:
    type: string
    description: |
      A non-navigable URI that identifies each error case.
  title:
    type: string
    description: |
      Error name.
  contractId:
    type: string
    description: |
      Contract that this occurred on.
  detail:
    type: string
    description: |
      Detailed description of the error.
  instance:
    type: string
    description: |
      Error instance.
  maximumManualConfigs:
    type: integer
    description: |
      Maximum allowed manual config entries on the contract for a given proxy zone.
  problemId:
    type: string
    description: |
      Problem instance ID.
  proxyId:
    type: integer
    description: |
      id
  status:
    type: integer
    description: |
      HTTP error status.
