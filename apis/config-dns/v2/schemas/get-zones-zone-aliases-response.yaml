type: object
description: |-
  A list of zone names that alias to this zone
  
x-akamai:
  file-path: schemas/get-zones-zone-aliases-response.yaml
additionalProperties: false
required:
- aliases
properties:
  aliases:
    type: array
    description: |
      A list of zone names.
    uniqueItems: true
    items:
      type: string
