type: object
description: |
  Zone names that alias to this zone.
additionalProperties: false
required:
- aliases
properties:
  aliases:
    type: array
    description: |
      Zone names.
    uniqueItems: true
    items:
      type: string
