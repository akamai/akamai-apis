type: object
description: Request to run a proposed proxy health check against a set of origin name servers.
required:
  - healthCheck
  - originNameservers
properties:
  healthCheck:
    $ref: proxy-healthcheck-get.yaml
  originNameservers:
    type: array
    description: List of master name servers to check.
    items:
      $ref: nameserver-get.yaml
additionalProperties: false