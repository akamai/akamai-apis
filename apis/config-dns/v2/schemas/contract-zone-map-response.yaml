type: object
description: |
  Maps contract IDs to zone names.
additionalProperties: false
required:
- contractId
- zoneNames
properties:
  contractId:
    type: string
    description: |
      The contract ID.
  zoneNames:
    type: array
    description: |
      Names of zones that are using the key.
    items:
      type: string
