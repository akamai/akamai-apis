type: object
description: |-
  The names or address of the master name servers to protect or transfer from.
  
x-akamai:
  file-path: schemas/nameserver-get.yaml
additionalProperties: false
properties:
  name:
    type: string
    description: |
      Master name server name or address.
  port:
    type: integer
    description: |
      The master name server port number.
    default: 53
