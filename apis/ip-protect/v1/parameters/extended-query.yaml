name: extended
in: query
description: |
  Enables inclusion of `ipsAllocated` and `ipsConfigured` values in the query response. These parameters are omitted from the API response if you don't include `extended` or set its value to `false`.
required: true
schema:
  type: boolean
  example: true
