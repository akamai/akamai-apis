name: endTime
in: query
description: |-
  Timestamp in Unix epoch milliseconds of the latest set of health statistics you want to return. If you omit the `endTime` parameter, the health status interval automatically extends to the current time.
  
x-akamai:
  file-path: parameters/endTime-query.yaml
schema:
  type: integer
  example: 1616693995000
