name: latest
in: query
description: |-
  Returns only the latest set of configuration values for the policy domain. Omit this parameter or set it to `false` to return all the available configuration value sets for the domain.
  
x-akamai:
  file-path: parameters/latest-query.yaml
required: true
schema:
  type: boolean
  example: true
