name: endTime
in: query
description: |
  Timestamp in Unix epoch milliseconds of the latest set of health statistics you want to return. If omitted, the health status interval automatically extends to the current time.
schema:
  type: integer
  example: 1616693995000
