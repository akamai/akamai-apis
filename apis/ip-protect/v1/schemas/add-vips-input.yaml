type: object
description: |
  Virtual IP addresses.
additionalProperties: false
required:
- vipsLimit
- portsLimit
properties:
  vipsLimit:
    type: integer
    description: |
      Number of virtual IP addresses you can allocate.
  portsLimit:
    type: integer
    description: |
      Number of network ports you can open on a single virtual IP address.
