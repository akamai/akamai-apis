type: array
description: |-
  Virtual IP addresses.
  
x-akamai:
  file-path: schemas/VipIdInput.yaml
items:
  type: object
  additionalProperties: false
  required:
  - vipIds
  properties:
    vipIds:
      type: number
      description: |
        Comma-delimited list of the unique identifiers of the virtual IP addresses to add.
