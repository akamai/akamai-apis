name: network
in: path
description: |-
  Network environment to execute the API request on.
  
x-akamai:
  file-path: parameters/network-path.yaml
required: true
schema:
  type: string
  example: production
  enum:
  - staging
  - production
