name: policyId
in: query
description: |-
  Filters the policy to specify a single image or video policy. To get a policy's ID, run the [List policies](ref:get-policies) operation. Use in conjunction with the `url`, `transformationType`, and `size` parameters.
  
x-akamai:
  file-path: parameters/detailsPolicyId-query.yaml
schema:
  type: string
  example: thumbnail_policy
