name: size
in: query
description: |-
  Size breakpoint, in pixels, configured in the policy. Use in conjunction with the `url`, `transformationType`, and `policyId` query parameters.
  
x-akamai:
  file-path: parameters/detailsSize-query.yaml
schema:
  type: integer
  example: 5000
