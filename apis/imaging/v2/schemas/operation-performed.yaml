type: object
description: |-
  The type of operation performed on an Image & Video Manager resource.
  
x-akamai:
  file-path: schemas/operation-performed.yaml
additionalProperties: false
properties:
  id:
    type: string
    description: |
      Unique identifier for a resource.
  description:
    type: string
    description: |
      The description of the resource.
  operationPerformed:
    type: string
    description: |
      The action performed on the resource, either `CREATED`, `UPDATED`, `DELETED`, or `CANCELLED`.
    enum:
    - CREATED
    - UPDATED
    - DELETED
    - CANCELLED
