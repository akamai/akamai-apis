type: object
description: |-
  Collects information that describes an image.
  
x-akamai:
  file-path: schemas/images-image.yaml
additionalProperties: false
properties:
  id:
    type: string
    description: |
      The ID of the image.
  errors:
    type: array
    description: |
      Any errors associated with an image.
    items:
      type: string
  url:
    type: string
    description: |
      The URL of the image.
