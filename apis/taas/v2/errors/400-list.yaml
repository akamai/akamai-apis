description: |
  [Bad request](ref:400). You provided bad parameter data or formatted the request body incorrectly.
content:
  application/problem+json:
    example:
      $ref: ../examples/add-revocation-list-response-400.json
    schema:
      $ref: ../schemas/error.yaml
