content:
    application/problem+json:
        example:
            $ref: ../examples/error-405.json
        schema:
            $ref: ../schemas/error.yaml
description: |
    [Not allowed](ref:405). You used an unsupported method with this operation. For example, you set the method to GET for a PUT operation.
