description: |-
  [Not found](ref:404). You've input the API URL incorrectly or named an asset that doesn't exist.
  
x-akamai:
  file-path: errors/404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/error-404.json
    schema:
      $ref: ../schemas/error.yaml
