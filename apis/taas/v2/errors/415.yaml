description: |-
  [Unsupported media type](ref:415). You've used an unsupported request or response content type, such as `application/xml`. Try the operation again using `application/json`.
  
x-akamai:
  file-path: errors/415.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/error-415.json
    schema:
      $ref: ../schemas/error.yaml
