type: object
description: |
  Details about an endpoint.
additionalProperties: false
properties:
  endpoints:
    type: array
    description: |
      The set of URL paths available for each stream for the specified time range.
    items:
      type: string
      title: Endpoint
