type: object
description: |
  Status of a VOD job.
additionalProperties: false
properties:
  status:
    type: string
    description: |
      The status of a request to create a new VOD asset. It can be `ready`, `in_progress`, or `failed`.
    enum:
    - in_progress
    - ready
    - failed
