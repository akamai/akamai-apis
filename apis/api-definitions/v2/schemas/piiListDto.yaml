type: object
description: |
  Contains information about the personally identifiable information (PII) found or defined in an API endpoint version.
x-akamai:
  file-path: schemas/piiListDto.yaml
additionalProperties: false
properties:
  piis:
    type: array
    description: |
      Collection of PII objects describing the kind of PII and where it appears in API payloads.
    items:
      $ref: piiDto.yaml
