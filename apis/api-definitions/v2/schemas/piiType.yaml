type: string
description: |
  The classification of PII. Currently only `PERSONAL_EMAIL` and `CREDIT_OR_DEBIT_CARD_NUMBER` are supported.
x-akamai:
  file-path: schemas/piiType.yaml
enum:
- PERSONAL_EMAIL
- CREDIT_OR_DEBIT_CARD_NUMBER
