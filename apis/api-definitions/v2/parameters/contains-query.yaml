name: contains
in: query
description: |-
  The search query substring criteria matching the endpoint's name, description, `basePath`, `apiCategoryName`, and `resourcePath`.
  
x-akamai:
  file-path: parameters/contains-query.yaml
schema:
  type: string
