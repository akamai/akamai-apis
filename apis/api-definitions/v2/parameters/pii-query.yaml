name: piiOnly
in: query
description: |-
  Whether to return only endpoints with personally identifiable information (PII).
  
x-akamai:
  file-path: parameters/pii-query.yaml
required: false
schema:
  type: boolean
  example: false
