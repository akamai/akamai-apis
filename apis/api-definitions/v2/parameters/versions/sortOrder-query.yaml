name: sortOrder
in: query
description: |
  The sort order, either `asc` for ascending or the default `desc` for descending.
schema:
  type: string
  example: desc
  default: desc
  enum:
  - asc
  - desc
