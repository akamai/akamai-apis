name: status
in: query
description: |-
  The status of an instance of personally identifiable information (PII) discovered on the network. See [PII status](ref:personally-identifiable-information-status).
  
x-akamai:
  file-path: parameters/piiStatus-query.yaml
required: false
schema:
  type: string
  example: DISCOVERED
  enum:
  - DISCOVERED
  - CONFIRMED
  - DEFERRED
