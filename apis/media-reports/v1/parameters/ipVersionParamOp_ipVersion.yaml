name: ipVersion
in: query
description: |-
  Limits reported data to an IP version, either `ipv4`, `ipv6`, or the default `all` for both versions.
  
x-akamai:
  file-path: parameters/ipVersionParamOp_ipVersion.yaml
schema:
  type: string
  example: all
  default: all
  enum:
  - ipv4
  - ipv6
  - all
