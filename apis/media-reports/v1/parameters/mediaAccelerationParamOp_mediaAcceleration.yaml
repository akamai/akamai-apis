name: mediaAcceleration
in: query
description: |-
  Limit reported data to media that is `accelerated` or `nonaccelerated`, or specify the default `all` for unlimited data.
  
x-akamai:
  file-path: parameters/mediaAccelerationParamOp_mediaAcceleration.yaml
schema:
  type: string
  example: accelerated
  default: all
  enum:
  - accelerated
  - nonaccelerated
  - all
