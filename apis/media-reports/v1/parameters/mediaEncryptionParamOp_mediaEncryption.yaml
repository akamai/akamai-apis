name: mediaEncryption
in: query
description: |-
  Limit reported data to media that is `encrypted` or `unencrypted` or specify the default `all` for unlimited data.
  
x-akamai:
  file-path: parameters/mediaEncryptionParamOp_mediaEncryption.yaml
schema:
  type: string
  example: encrypted
  default: all
  enum:
  - encrypted
  - unencrypted
  - all
