name: deliveryType
in: query
description: |-
  Limits data to the media's delivery context, either `live`, `vod` (video on demand), or the default `all`.
  
x-akamai:
  file-path: parameters/deliveryTypeParamOp_deliveryType.yaml
schema:
  type: string
  example: all
  default: all
  enum:
  - live
  - vod
  - all
