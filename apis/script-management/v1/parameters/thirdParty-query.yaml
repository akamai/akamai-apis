name: thirdParty
in: query
description: |-
  Set to `false` to exclude third-party scripts. If you leave it out, the operation includes third-party scripts in the response.
  
x-akamai:
  file-path: parameters/thirdParty-query.yaml
schema:
  type: boolean
  example: false
  default: true
