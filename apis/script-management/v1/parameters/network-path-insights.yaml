name: network
in: path
description: |-
  The Akamai network where the Ion property with Script Management enabled applies: `staging`, `production`, or `both`.
  
x-akamai:
  file-path: parameters/network-path-insights.yaml
required: true
schema:
  type: string
  example: production
  enum:
  - staging
  - production
  - both
