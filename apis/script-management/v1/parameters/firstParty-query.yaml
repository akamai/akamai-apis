name: firstParty
in: query
description: |-
  Set to `false` to exclude first-party scripts. If you leave it out, the operation includes first-party scripts in the response.
  
x-akamai:
  file-path: parameters/firstParty-query.yaml
schema:
  type: boolean
  example: false
  default: true
