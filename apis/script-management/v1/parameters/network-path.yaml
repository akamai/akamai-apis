name: network
in: path
description: |
  The Akamai network where the Script Management policy applies: `staging`, `production`, or `both`.
required: true
schema:
  type: string
  example: production
  enum:
  - staging
  - production
  - both
