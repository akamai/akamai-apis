name: pathPrefix
in: path
description: |-
  The path prefix to match against the object name of events to determine if there should be a notification. Starts after the CP code of the root directory. Can be empty.
  
x-akamai:
  file-path: parameters/pathPrefix.yaml
required: true
schema:
  type: string
  example: directory
  minLength: 0
