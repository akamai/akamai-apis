type: object
description: |
  Test.
additionalProperties: false
properties:
  subscriptions:
    type: array
    description: |
      Displays subscriptions for a service.
    items:
      $ref: subscription.yaml
