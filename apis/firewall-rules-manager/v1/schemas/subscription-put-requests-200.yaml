type: object
description: |
  Test.
additionalProperties: false
properties:
  subscriptions:
    type: array
    description: |
      Information necessary to include when subscribing users to CIDR block notifications for a service.
    items:
      $ref: subscription-put-request.yaml
