openapi: 3.0.0
info:
  title: Configurations
  version: '1'
servers:
- url: https://{hostname}/client-access-control/v1
x-akamai:
  auth-type: EDGE_GRID
paths:
  /configurations:
    $ref: paths/configurations.yaml
  /configurations/{configurationId}:
    $ref: paths/configuration.yaml
  /configurations/{configurationId}/acknowledge/{version}:
    $ref: paths/version.yaml
tags:
  - name: CAC configurations
    description: |
      Get information about your Client Access Control (CAC) configurations.
  - name: CIDR blocks
    description: |
      Acknowledge proposed CIDR blocks.
