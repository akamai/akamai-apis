type: array
x-akamai:
  file-path: schemas/ListOfMoveUserResponse.yaml
minItems: 0
items:
  type: object
  additionalProperties: false
  required:
  - accountId
  - email
  - firstName
  - lastLoginDate
  - lastName
  - uiIdentityId
  - uiUserName
  properties:
    accountId:
      type: string
      description: |
        The user's account ID.
      minLength: 1
    email:
      type: string
      description: |
        The user's email address.
      minLength: 1
    firstName:
      type: string
      description: |
        The user's first name.
      minLength: 1
    lastLoginDate:
      type: string
      description: |
        ISO 8601 timestamp indicating when the user last logged in.
      format: date-time
    lastName:
      type: string
      description: |
        The user's surname.
      minLength: 1
    uiIdentityId:
      type: string
      description: |
        The user's ACC ID.
      minLength: 1
    uiUserName:
      type: string
      description: |
        The user's ACC username.
      minLength: 1
