description: 'Encapsulates all the users who needs to be blocked based on UI identities.

  '
items:
  $ref: user-block-uiidentities.yaml
minItems: 0
type: array
x-akamai:
  file-path: schemas/users-block-uiidentities.yaml
