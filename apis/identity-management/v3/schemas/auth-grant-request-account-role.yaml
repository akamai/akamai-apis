additionalProperties: false
description: The user's account level role assignments.
properties:
  roleDescription:
    description: 'Descriptive label for the role to convey its use.

      '
    minLength: 1
    type: string
  roleId:
    description: 'Unique identifier for each role. To get this value, run the [List
      roles](ref:get-roles) operation and look for roles with the `account` type.

      '
    minimum: 1
    type: integer
  roleName:
    description: 'Descriptive label for the role.

      '
    minLength: 1
    type: string
required:
- roleId
title: Account level role
type: object
x-akamai:
  file-path: schemas/auth-grant-request-account-role.yaml
