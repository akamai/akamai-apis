additionalProperties: false
description: 'A user''s role assignments, per group.

  '
properties:
  groupId:
    description: 'Unique identifier for each group. It''s returned only for group
      roles.

      '
    minimum: 0
    type: integer
  groupName:
    description: 'Descriptive label for the group.

      '
    minLength: 1
    type: string
  isBlocked:
    description: 'Whether a user''s access to a group is blocked.

      '
    type: boolean
  roleDescription:
    description: 'Descriptive label for the role to convey its use.

      '
    minLength: 1
    type: string
  roleId:
    description: 'Unique identifier for each role.

      '
    minimum: 1
    type: integer
  roleName:
    description: 'Descriptive label for the role.

      '
    minLength: 1
    type: string
  subGroups:
    description: 'Children of the parent group. Permissions cascade downward from
      parent to child unless the child group''s `isBlocked` is `true`.

      '
    items:
      $ref: groups-nested.yaml
    minItems: 0
    type: array
required:
- roleId
- roleName
type: object
x-akamai:
  file-path: schemas/auth-grant-response.yaml
