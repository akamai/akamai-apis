additionalProperties: false
properties:
  grantedRoles:
    description: 'Granted roles for the account.

      '
    items:
      additionalProperties: false
      properties:
        grantedRoleId:
          description: 'Unique identifier for the grantable role. To get this value,
            run the [List grantable roles](ref:get-grantable-roles) operation.

            '
          minimum: 1
          type: integer
      required:
      - grantedRoleId
      type: object
    minItems: 0
    type: array
  roleDescription:
    description: 'Descriptive label for the role to convey its use.

      '
    maxLength: 250
    minLength: 1
    type: string
  roleName:
    description: 'Descriptive label for the role.

      '
    maxLength: 70
    minLength: 1
    type: string
required:
- roleName
- roleDescription
- grantedRoles
type: object
x-akamai:
  file-path: schemas/role-request-post.yaml
