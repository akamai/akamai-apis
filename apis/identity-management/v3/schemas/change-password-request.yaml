additionalProperties: false
description: 'Uses the old password to authenticate you are who you say you are when
  you update your password. Includes the new password.

  '
properties:
  currentPassword:
    description: 'Your existing password.

      '
    minLength: 1
    type: string
  newPassword:
    description: 'New password.

      '
    minLength: 1
    type: string
required:
- currentPassword
- newPassword
type: object
x-akamai:
  file-path: schemas/change-password-request.yaml
