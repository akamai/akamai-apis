type: object
description: |-
  Includes the new `groupName` you're using to replace the old `groupName`.
  
x-akamai:
  file-path: schemas/group-request.yaml
additionalProperties: false
required:
- groupName
properties:
  groupName:
    type: string
    description: |
      Descriptive label for the group.
    minLength: 1
    maxLength: 50
