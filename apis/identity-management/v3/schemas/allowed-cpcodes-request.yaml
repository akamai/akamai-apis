additionalProperties: false
description: 'POST allowed CP codes.

  '
properties:
  clientType:
    default: CLIENT
    description: 'Specifies the API client''s ownership and credential management.
      `CLIENT` indicates the creator owns and manages the credentials. `USER_CLIENT`
      indicates another user owns the client and manages the credentials. `SERVICE_ACCOUNT`
      indicates only `authorizedUsers` can manage the client''s credentials. The default
      is `CLIENT`.

      '
    enum:
    - CLIENT
    - USER_CLIENT
    - SERVICE_ACCOUNT
    type: string
  groups:
    description: 'Groups the API client can access.

      '
    items:
      $ref: groups.yaml#/definitions/groups
    minItems: 0
    type: array
required:
- clientType
- groups
type: object
x-akamai:
  file-path: schemas/allowed-cpcodes-request.yaml
