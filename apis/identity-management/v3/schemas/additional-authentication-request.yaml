additionalProperties: false
properties:
  value:
    description: 'A user''s multi-factor authentication method, either `TFA`, `MFA`,
      or `NONE`.

      '
    enum:
    - TFA
    - MFA
    - NONE
    type: string
required:
- value
type: object
x-akamai:
  file-path: schemas/additional-authentication-request.yaml
