type: object
x-akamai:
  file-path: schemas/additional-authentication-request.yaml
additionalProperties: false
required:
- value
properties:
  value:
    type: string
    description: |
      A user's multi-factor authentication method, either `TFA`, `MFA`, or `NONE`.
    enum:
    - TFA
    - MFA
    - NONE
