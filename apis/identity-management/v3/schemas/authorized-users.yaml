description: 'Authorized user on an account.

  '
items:
  additionalProperties: false
  properties:
    email:
      description: 'The user''s email address.

        '
      format: email
      type: string
    firstName:
      description: 'The user''s first name.

        '
      minLength: 1
      type: string
    lastName:
      description: 'The user''s surname.

        '
      minLength: 1
      type: string
    uiIdentityId:
      description: 'Unique identifier for each user, which corresponds to their Control
        Center profile or client ID. Also known as a `contactId` in other APIs.

        '
      minLength: 1
      type: string
    username:
      description: 'The user''s Control Center sign-in name.

        '
      minLength: 1
      type: string
  required:
  - email
  - firstName
  - lastName
  - uiIdentityId
  - username
  type: object
minItems: 0
type: array
x-akamai:
  file-path: schemas/authorized-users.yaml
