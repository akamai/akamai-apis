type: array
description: |-
  A schema for name value pair.
  
x-akamai:
  file-path: schemas/AllowedCpcodesResponse.yaml
minItems: 0
items:
  type: object
  additionalProperties: false
  required:
  - name
  - value
  properties:
    name:
      type: string
      description: |
        Descriptive label for the CP code.
      minLength: 1
    value:
      type: integer
      description: |
        Unique identifier for the CP code.
      minimum: 0
