additionalProperties: false
description: 'Specifies time zones that can be assigned to each user.

  '
properties:
  description:
    description: 'Descriptive label for the time zone, including the `+` or `-` offset
      from GMT.

      '
    minLength: 1
    type: string
  offset:
    description: 'The time zone offset from GMT.

      '
    minLength: 1
    type: string
  posix:
    description: 'The time zone posix.

      '
    minLength: 1
    type: string
  timezone:
    description: 'The time zone ID.

      '
    minLength: 1
    type: string
required:
- timezone
- description
type: object
x-akamai:
  file-path: schemas/time-zone.yaml
