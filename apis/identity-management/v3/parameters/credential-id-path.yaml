description: 'Unique identifier for each credential. To get this value for your credentials,
  run the [List your credentials](ref:get-self-credentials) operation. For other users''
  credentials, run the [List credentials](ref:get-client-credentials) operation.

  '
in: path
name: credentialId
required: true
schema:
  example: 123456
  type: integer
x-akamai:
  file-path: parameters/credential-id-path.yaml
