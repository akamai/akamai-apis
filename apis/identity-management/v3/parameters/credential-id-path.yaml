name: credentialId
in: path
description: |-
  Unique identifier for each credential. To get this value for your credentials, run the [List your credentials](ref:get-self-credentials) operation. For other users' credentials, run the [List credentials](ref:get-client-credentials) operation.
  
x-akamai:
  file-path: parameters/credential-id-path.yaml
required: true
schema:
  type: integer
  example: 123456
