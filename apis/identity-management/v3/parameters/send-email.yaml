name: sendEmail
in: query
description: |-
  Sends a one-time password to the new user, `true` by default.
  
x-akamai:
  file-path: parameters/send-email.yaml
schema:
  type: boolean
  example: false
  default: true
