name: cidrblock
in: query
description: |-
  CIDR block or any IP address value you want to validate. Encode any slash characters as `%2F`. This isn't the same as a `cidrBlockId`.
  
x-akamai:
  file-path: parameters/cidr-block-query.yaml
required: true
schema:
  type: string
  example: 127.0.0.1/20
