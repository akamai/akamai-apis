description: 'CIDR block or any IP address value you want to validate. Encode any
  slash characters as `%2F`. This isn''t the same as a `cidrBlockId`.

  '
in: query
name: cidrblock
required: true
schema:
  example: 127.0.0.1/20
  type: string
x-akamai:
  file-path: parameters/cidr-block-query.yaml
