name: groupAccess
in: query
description: |-
  Includes the API client's group access, `false` by default.
  
x-akamai:
  file-path: parameters/group-access-query.yaml
schema:
  type: boolean
  example: true
  default: false
