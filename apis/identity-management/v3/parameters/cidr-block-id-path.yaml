description: 'Unique identifier for each CIDR block. To get this value, run the [List
  CIDR blocks](ref:get-allowlist) operation.

  '
in: path
name: cidrBlockId
required: true
schema:
  example: 6688
  type: integer
x-akamai:
  file-path: parameters/cidr-block-id-path.yaml
