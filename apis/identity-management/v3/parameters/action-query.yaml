name: action
in: query
description: |-
  Includes actions you can perform for 2FA on your profile, either `enable`, `disable`, or `reset`. Don't confuse this with the `actions` parameter, which applies to the various operations that get user data.
  
x-akamai:
  file-path: parameters/action-query.yaml
required: true
schema:
  type: string
  example: enable
  enum:
  - enable
  - disable
  - reset
