name: allowAccountSwitch
in: query
description: |-
  Optionally enable `allowAccountSwitch` to include them as part of the response object.
  
x-akamai:
  file-path: parameters/allowAccountSwitch-query.yaml
schema:
  type: boolean
  example: true
