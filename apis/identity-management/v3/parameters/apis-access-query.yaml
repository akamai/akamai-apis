name: apiAccess
in: query
description: |-
  Lists only the APIs the user can access, `false` by default.
  
x-akamai:
  file-path: parameters/apis-access-query.yaml
schema:
  type: boolean
  example: true
  default: false
