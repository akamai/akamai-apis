description: 'Filters data by client type, either `USER_CLIENT`, `SERVICE_ACCOUNT`,
  or default `CLIENT`.

  '
in: query
name: clientType
schema:
  default: CLIENT
  enum:
  - USER_CLIENT
  - SERVICE_ACCOUNT
  - CLIENT
  example: CLIENT
  type: string
x-akamai:
  file-path: parameters/client-type-query.yaml
