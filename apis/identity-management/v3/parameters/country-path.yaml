name: country
in: path
description: |-
  Filters the results by country, but only returns lists of states and provinces for `USA` and `Canada`.
  
x-akamai:
  file-path: parameters/country-path.yaml
required: true
schema:
  type: string
  example: canada
