description: 'Filters the results by country, but only returns lists of states and
  provinces for `USA` and `Canada`.

  '
in: path
name: country
required: true
schema:
  example: canada
  type: string
x-akamai:
  file-path: parameters/country-path.yaml
