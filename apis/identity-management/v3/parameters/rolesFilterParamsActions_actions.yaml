name: actions
in: query
description: |-
  When enabled, the response includes information about actions such as `edit` or `delete` that you can take for the object. Not to be confused with the `action` parameter, which applies to the [Set two-factor authentication](ref:put-user-profile-tfa) operation.
  
x-akamai:
  file-path: parameters/rolesFilterParamsActions_actions.yaml
required: true
schema:
  type: boolean
  example: true
  default: false
