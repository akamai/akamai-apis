name: actions
in: query
description: |-
  When enabled, the response includes actions such as `edit` or `delete` that a user can perform on an object. Don't confuse this with the `action` parameter, which applies to the [Set two-factor authentication](ref:put-user-profile-tfa) operation.
  
x-akamai:
  file-path: parameters/adminActions_actions.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
