description: 'Filters results by `accountId` or `accountName`. Enter at least three
  characters to filter the results by substring.

  '
in: query
name: search
schema:
  example: indirect
  minLength: 3
  type: string
x-akamai:
  file-path: parameters/account-switch-keys-query.yaml
