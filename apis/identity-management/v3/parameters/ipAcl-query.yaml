name: ipAcl
in: query
description: |-
  Optionally enable `ipAcl` to include them as part of the response object.
  
x-akamai:
  file-path: parameters/ipAcl-query.yaml
schema:
  type: boolean
  example: true
