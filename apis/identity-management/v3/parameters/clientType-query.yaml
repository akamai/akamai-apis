name: clientType
in: query
description: |
  Filters data by client type, either `CLIENT`, `USER_CLIENT`, or `SERVICE_ACCOUNT`. 
x-akamai:
  file-path: parameters/clientType-query.yaml
schema:
  type: string
  example: CLIENT
  default: CLIENT
  enum:
  - CLIENT
  - USER_CLIENT
  - SERVICE_ACCOUNT
