name: credentials
in: query
description: |-
  Optionally enable `credentials` to include them as part of the response object.
  
x-akamai:
  file-path: parameters/credentials-query.yaml
schema:
  type: boolean
  example: true
