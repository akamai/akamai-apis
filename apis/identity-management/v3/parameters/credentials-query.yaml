name: credentials
in: query
description: |-
  Includes API user's credentials, `false` by default.
  
x-akamai:
  file-path: parameters/credentials-query.yaml
schema:
  type: boolean
  example: true
  default: false
