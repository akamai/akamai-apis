description: 'Returns all the roles for the current account, ignoring the contract
  type of your API client, `false` by default.

  '
in: query
name: ignoreContext
required: false
schema:
  default: false
  example: true
  type: boolean
x-akamai:
  file-path: parameters/roles-filter-params-context-ignore-context.yaml
