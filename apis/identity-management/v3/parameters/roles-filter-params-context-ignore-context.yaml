name: ignoreContext
in: query
description: |-
  Returns all the roles for the current account, ignoring the contract type of your API client, `false` by default.
  
x-akamai:
  file-path: parameters/roles-filter-params-context-ignore-context.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
