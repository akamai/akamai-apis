name: ipAcl
in: query
description: |-
  Includes the API client's IP list restriction, `false` by default.
  
x-akamai:
  file-path: parameters/ip-acl-query.yaml
schema:
  type: boolean
  example: true
  default: false
