description: 'Includes information about actions such as `edit` or `delete` that you
  can take for the object, `false` by default. Don''t confuse this with the `action`
  parameter, which applies to the [Set two-factor authentication](ref:put-user-profile-tfa)
  operation.

  '
in: query
name: actions
schema:
  default: false
  example: true
  type: boolean
x-akamai:
  file-path: parameters/roles-filter-params-actions-actions.yaml
