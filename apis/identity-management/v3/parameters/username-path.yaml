description: 'Unique username for each user. To get this value, run the [List authorized
  users](ref:get-api-clients-users) operation.

  '
in: path
name: username
required: true
schema:
  example: jsmith
  type: string
x-akamai:
  file-path: parameters/username-path.yaml
