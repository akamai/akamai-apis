name: actions
in: query
description: |-
  When enabled, the response includes information about actions such as `edit` or `delete` that you can take for the object. In this case, users. Not to be confused with the `action` parameter, which applies to the [Set two-factor authentication](ref:put-user-profile-tfa) operation.
  
x-akamai:
  file-path: parameters/adminActions_actions.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
