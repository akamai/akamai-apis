type: object
description: |-
  Describes the request body for moving a property under another group.
  
x-akamai:
  file-path: schemas/MovePropertyRequest.yaml
additionalProperties: false
required:
- sourceGroupId
- destinationGroupId
properties:
  destinationGroupId:
    type: integer
    description: |
      Identifies the group to which you want to move the property.
  sourceGroupId:
    type: integer
    description: |
      Identifies the group from which you want to move the property.
