type: object
additionalProperties: false
properties:
  isBlocked:
    type: boolean
    description: |
      Indicates whether a user's access to a group is blocked.
  roleId:
    type: integer
    description: |
      A unique identifier for each role.
  groupId:
    type: integer
    description: |
      A unique identifier for each group.
  groupName:
    type: string
    description: |
      A group's name.
  roleName:
    type: string
    description: |
      The role's name.
  roleDescription:
    type: string
    description: |
      Descriptive text for the role.
  subGroups:
    type: array
    description: |
      Array of nested groups.
    items:
      $ref: auth-grant-request-definitions.yaml#/definitions/AuthGrantRequest
