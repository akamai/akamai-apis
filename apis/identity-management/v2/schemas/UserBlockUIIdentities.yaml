type: object
description: |-
  This object encapsulates all the users who needs to be blocked based on UI identities.
  
x-akamai:
  file-path: schemas/UserBlockUIIdentities.yaml
additionalProperties: false
required:
- uiIdentityId
properties:
  uiIdentityId:
    type: string
    description: |
      A unique identifier for a user. Also known as a `contactId` in other APIs.
    readOnly: true
