type: object
description: |-
  Includes the new `groupName` you are using to replace the old `groupName`.
  
x-akamai:
  file-path: schemas/GroupRequest.yaml
additionalProperties: false
required:
- groupName
properties:
  groupName:
    type: string
    description: |
      The human-readable name for a group.
    minLength: 1
    maxLength: 50
