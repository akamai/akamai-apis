type: object
description: |
  Encapsulates the status of IP allowlist on the account.
additionalProperties: false
required:
- enabled
properties:
  enabled:
    type: boolean
    description: |
      Indicates whether IP allowlist is enabled on the account.
