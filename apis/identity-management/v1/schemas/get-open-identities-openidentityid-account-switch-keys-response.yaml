type: array
x-akamai:
  file-path: schemas/get-open-identities-openidentityid-account-switch-keys-response.yaml
items:
  type: object
  description: |
    This object encapsulates a collection of contexts the open client can manage.
  properties:
    accountName:
      type: string
      description: |
        The name you supply for an account.
    accountSwitchKey:
      type: string
      description: |
        The identifier for an account other than your API client's default.
