name: includeContext
in: query
description: |-
  Includes test run context in the response.
  
x-akamai:
  file-path: parameters/includeContext-query.yaml
schema:
  type: boolean
  example: true
  default: true
