name: pageSize
in: query
description: |-
  The number of items on each result page. The default value is `1000`.
  
x-akamai:
  file-path: parameters/pageSize-query.yaml
schema:
  type: integer
  example: 10
  default: 1000
