name: includeSkipped
in: query
description: |-
  Includes details on objects skipped during the test run in the response.
  
x-akamai:
  file-path: parameters/includeSkipped-query.yaml
schema:
  type: boolean
  example: true
  default: false
