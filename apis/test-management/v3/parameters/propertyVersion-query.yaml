name: propertyVersion
in: query
description: |-
  Version of the property associated with the test suite to filter the list by. Combine this parameter with `propertyId` or `propertyName`.
  
x-akamai:
  file-path: parameters/propertyVersion-query.yaml
schema:
  type: integer
  example: 4
