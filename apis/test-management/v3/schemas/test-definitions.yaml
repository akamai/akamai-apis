type: object
description: |-
  Lists all the test definition created for comparative testing.
  
x-akamai:
  file-path: schemas/test-definitions.yaml
additionalProperties: false
required:
- testDefinitions
properties:
  testDefinitions:
    type: array
    description: |
      List of all the test definition created for comparative testing.
    items:
      $ref: test-definition.yaml
