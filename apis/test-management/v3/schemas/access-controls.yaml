type: object
description: |-
  Contains access control objects of users or groups for a locked test suite.
  
x-akamai:
  file-path: schemas/access-controls.yaml
additionalProperties: false
required:
- accessControls
properties:
  accessControls:
    type: array
    minItems: 0
    items:
      $ref: access-control.yaml
