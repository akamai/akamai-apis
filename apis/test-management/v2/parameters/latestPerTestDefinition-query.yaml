name: latestPerTestDefinition
in: query
description: |-
  Whether the response includes only the latest test definition execution for each passed valid test definition identifier. It's `false` by default.
  
x-akamai:
  file-path: parameters/latestPerTestDefinition-query.yaml
schema:
  type: boolean
  example: true
  default: false
