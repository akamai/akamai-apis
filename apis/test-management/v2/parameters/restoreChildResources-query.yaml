name: restoreChildResources
in: query
description: |-
  Whether to restore the test suite with included functional test cases, test requests, and conditions. It's `false` by default.
  
x-akamai:
  file-path: parameters/restoreChildResources-query.yaml
schema:
  type: boolean
  example: true
  default: false
