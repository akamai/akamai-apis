name: includeRecentlyDeleted
in: query
description: |-
  Whether to list any items deleted within the last 30 days that you can restore. It's `false` by default.
  
x-akamai:
  file-path: parameters/includeRecentlyDeleted-query.yaml
schema:
  type: boolean
  example: true
  default: false
