name: deleteChildResources
in: query
description: |-
  Whether to delete the test suite with included functional test cases, test requests, and conditions. It's `false` by default.
  
x-akamai:
  file-path: parameters/deleteChildResources-query.yaml
schema:
  type: boolean
  example: true
  default: false
