type: array
description: |-
  The list of differences in the test run in the two environments.
  
x-akamai:
  file-path: schemas/getDifferencesResponse.yaml
items:
  $ref: ../schemas/get-difference-response.yaml
