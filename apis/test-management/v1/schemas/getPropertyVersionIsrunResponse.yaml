type: object
description: |-
  Verify if propertyId and version are tested in ATC.
  
x-akamai:
  file-path: schemas/getPropertyVersionIsrunResponse.yaml
additionalProperties: false
required:
- propertyId
- tested
- version
properties:
  version:
    type: integer
    description: |
      Version of the property.
  propertyId:
    type: integer
    description: |
      The unique identifier for the property.
  tested:
    type: boolean
    description: |
      Status of propertyId and version tested in ATC.
