type: array
description: |-
  Create notes for differences in bulk.
  
x-akamai:
  file-path: schemas/create-diff-note-request.yaml
items:
  type: object
  additionalProperties: false
  required:
  - differenceId
  - note
  properties:
    differenceId:
      type: string
    note:
      type: string
