type: object
x-akamai:
  file-path: schemas/hostname-provision-state-changes-current-get-response.yaml
additionalProperties: false
properties:
  provisionStateChanges:
    type: array
    description: |
      Information about the change made to the property hostname's deployment in China.
    items:
      $ref: provision-state-change.yaml
