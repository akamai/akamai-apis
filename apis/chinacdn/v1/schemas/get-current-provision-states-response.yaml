type: object
x-akamai:
  file-path: schemas/get-current-provision-states-response.yaml
additionalProperties: true
required:
- provisionStates
properties:
  provisionStates:
    type: array
    description: |
      The provision states for all property hostnames under your account.
    items:
      $ref: provisionState.yaml
