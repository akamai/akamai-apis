type: object
x-akamai:
  file-path: schemas/provision-states-current-get-response.yaml
additionalProperties: true
required:
- provisionStates
properties:
  provisionStates:
    type: array
    description: |
      The provision states for all property hostnames under your account.
    items:
      $ref: provision-state.yaml
