name: jurisdiction
in: path
description: |-
  The name of a jurisdiction. The following options are available: `na` for North America, `eu` for Europe, `jp` for Japan, or `in` for India.
  
x-akamai:
  file-path: parameters/jurisdiction-path.yaml
required: true
schema:
  type: string
  example: na
  enum:
  - na
  - eu
  - jp
  - in
