name: activation-state
in: query
description: |-
  Specify `activated` as a keyword to activate the specified configuration version.
  
x-akamai:
  file-path: parameters/activation-state-query.yaml
required: true
schema:
  type: string
  example: activated
  enum:
  - activated
  - deactivated
