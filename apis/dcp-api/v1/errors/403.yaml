description: |-
  Access denied, the user has no right to perform the operation, or the affected resource belongs to a different customer or account.
  
x-akamai:
  file-path: errors/403.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/exampleForbidden403ErrorRS.json
    schema:
      $ref: ../schemas/httpProblemDetails.yaml
