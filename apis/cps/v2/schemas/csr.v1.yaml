type: object
description: |
  Certificate Signing Request (CSR).
additionalProperties: false
required:
- csr
properties:
  csr:
    type: string
    description: |
      String with PEM formatted CSR.
    nullable: true
    minLength: 1
