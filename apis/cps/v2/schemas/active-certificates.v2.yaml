type: object
description: |
  Lists enrollments with details on active certificates.
additionalProperties: false
required:
- enrollments
properties:
  enrollments:
    type: array
    description: |
      Enrollments with active certificates.
    items:
      $ref: active-certificate.v2.yaml
