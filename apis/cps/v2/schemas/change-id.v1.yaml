type: object
description: |
  The change for this enrollment on which to perform the desired operation.
additionalProperties: false
required:
- change
properties:
  change:
    type: string
    description: |
      Identifies the change to the enrollment.
