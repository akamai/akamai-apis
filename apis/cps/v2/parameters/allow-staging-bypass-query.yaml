name: allow-staging-bypass
in: query
description: |-
  Bypass staging and push meta-data updates directly to production network. Current change will also be updated with the same changes.
  
x-akamai:
  file-path: parameters/allow-staging-bypass-query.yaml
schema:
  type: boolean
  example: true
