x-akamai:
  path-info: /enrollments/{enrollmentId}/deployments/production
parameters:
- $ref: ../parameters/enrollment-id-path.yaml
get:
  operationId: get-deployments-production
  summary: Get production deployment
  tags:
  - Deployments
  description: |
    Gets the enrollments deployed on the production network.
  responses:
    200:
      description: |
        The response reflects your deployment's details to the production network.
      content:
        application/vnd.akamai.cps.deployment.v8+json:
          example:
            $ref: ../examples/deployment.v8.json
          schema:
            $ref: ../schemas/deployment.v8.yaml
