type: array
description: |-
  Contains information about the CDN.
  
x-akamai:
  file-path: schemas/CdnResourceDTO.yaml
items:
  type: object
  additionalProperties: false
  properties:
    name:
      type: string
      description: |
        Name to identify the CDN.
    code:
      type: string
      description: |
        Predefined code to identify the CDN.
