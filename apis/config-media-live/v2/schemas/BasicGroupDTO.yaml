type: object
description: |-
  Specify the group ID to associate with the contract.
  
x-akamai:
  file-path: schemas/BasicGroupDTO.yaml
additionalProperties: false
properties:
  groupId:
    type: integer
    description: |
      Specify the group ID.
  groupName:
    type: string
    description: |
      Specify the group name.
