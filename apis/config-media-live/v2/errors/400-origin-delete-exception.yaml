description: |-
  [Invalid data input](ref:400). The API returns 400 response when the user doesn't have access to the requested origin or when there are streams in the origin.
  
x-akamai:
  file-path: errors/400-origin-delete-exception.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/OriginDeleteException.json
    schema:
      $ref: ../schemas/ProblemDetailDTO.yaml
