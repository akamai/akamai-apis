description: |-
  [Invalid data input](ref:400). The API returns 400 response when the user doesn't have access to the requested stream ID or when an invalid stream ID is passed.
  
x-akamai:
  file-path: errors/400-stream-get-exception.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/StreamGetException.json
    schema:
      $ref: ../schemas/ProblemDetailDTO.yaml
