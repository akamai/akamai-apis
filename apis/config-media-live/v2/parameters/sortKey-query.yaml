name: sortKey
in: query
description: |-
  Sorts the fields displayed based on the key that you specify, either `cpcode`, `createdDate`, `dvrWindowInMin`, `format`, `modifiedDate`, `name`, `originHostName`, `status`, or `zone`.
  
x-akamai:
  file-path: parameters/sortKey-query.yaml
schema:
  type: string
  example: createdDate
  default: modifiedDate
  enum:
  - name
  - format
  - cpcode
  - zone
  - originHostName
  - status
  - dvrWindowInMin
  - createdDate
  - modifiedDate
