name: type
in: query
description: |-
  Specify the type of CP code. Specify `INGEST` to obtain the INGEST CP codes, `DELIVERY` for the delivery CP codes, and `STORAGE` for the storage CP codes.
  
x-akamai:
  file-path: parameters/type-cpcodes-query.yaml
required: true
schema:
  type: string
  example: INGEST
  enum:
  - INGEST
  - DELIVERY
  - STORAGE
