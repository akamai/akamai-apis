name: unused
in: query
description: |-
  When enabled, lists only CP codes that have not already been used to provision an origin.
  
x-akamai:
  file-path: parameters/unused-query.yaml
schema:
  type: boolean
  example: true
