type: object
x-akamai:
  file-path: schemas/rule-sets-get.yaml
additionalProperties: false
properties:
  items:
    type: array
    description: |
      A set of Access Control List Rule Set objects. ACLs are collections of IP addresses or geographic regions that you create to either allow or block their access to a storage group.
    items:
      $ref: rule-set-get.yaml
