description: |-
  Enum definitions for the Activation resource.
  
x-akamai:
  file-path: schemas/activation-enum-definitions.yaml
definitions:
  network:
    type: string
    enum:
    - STAGING
    - PRODUCTION
  revisionStatus:
    type: string
    enum:
    - PRESUBMIT
    - PENDING
    - IN_PROGRESS
    - ABORTED
    - ERROR
    - COMPLETE
    - DELETED_BY_AKAMAI
    - EXPIRED
    - CANCELLED
    - INVALID_COMBINED_BUNDLE
  status:
    type: string
    enum:
    - PRESUBMIT
    - PENDING
    - IN_PROGRESS
    - ABORTED
    - ERROR
    - COMPLETE
    - DELETED_BY_AKAMAI
    - EXPIRED
    - CANCELLED
