type: object
description: |-
  This object collects data about any properties that enable EdgeWorkers.
  
x-akamai:
  file-path: schemas/properties-get-200.yaml
additionalProperties: false
required:
- properties
properties:
  properties:
    type: array
    description: |
      List of properties.
    minItems: 0
    items:
      $ref: property-get-200.yaml
  limitedAccessToProperties:
    type: boolean
    description: |
      Whether you have a limited access to properties. Only properties you have access to are listed.
