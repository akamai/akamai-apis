name: includePinnedRevisions
in: query
description: |-
  When enabled, limits results to show only the pinned revisions when the BOM was created.
  
x-akamai:
  file-path: parameters/includePinnedRevisions-query.yaml
schema:
  type: boolean
  example: true
