name: includeActiveVersions
in: query
description: |-
  When enabled, limits results to show only active versions.
  
x-akamai:
  file-path: parameters/includeActiveVersions-query.yaml
schema:
  type: boolean
  example: true
