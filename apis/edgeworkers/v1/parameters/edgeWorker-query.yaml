name: edgeWorker
in: query
description: |-
  Filter by EdgeWorker ID such as `42` or more specific version such as `42-1.0`.
  
x-akamai:
  file-path: parameters/edgeWorker-query.yaml
required: true
schema:
  type: string
  example: 42
