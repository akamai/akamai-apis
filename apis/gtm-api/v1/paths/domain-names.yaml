x-akamai:
  path-info: /domain-list/domain-names
get:
  operationId: get-domain-names
  summary: List domain names
  tags:
  - Domains
  description: |
    Returns a list of GTM domain names accessible to the current user.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-domain-names-200.json
          schema:
            $ref: ../schemas/domain-names.yaml
