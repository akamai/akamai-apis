x-akamai:
  path-info: /domain-list
get:
  operationId: get-domain-list
  summary: Get a domain list
  tags:
  - Domains
  description: |
    This operation retrieves a list of domains, along with a URL templated link to obtain a domain summary of associated properties and data centers for each.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-domain-list-200.json
          schema:
            $ref: ../schemas/domain-list.yaml
