x-akamai:
  path-info: /diagnostics/mapdiff/domains/{domain}/datacenters/{datacenterId}/as/{asNumber}
parameters:
- $ref: ../parameters/domain-path.yaml
- $ref: ../parameters/datacenter-id-path.yaml
- $ref: ../parameters/as-number-path.yaml
get:
  operationId: get-diagnostics-mapdiff-datacenter-as
  summary: Report detailed map diff diagnostics for a data center and AS
  tags:
  - Diagnostics
  description: |
    Returns detailed geo and ping score time series for the given domain, datacenter, and autonomous system over the specified time period.
  parameters:
  - $ref: ../parameters/start-query.yaml
  - $ref: ../parameters/end-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/get-diagnostics-mapdiff-detailed-200.json
          schema:
            $ref: ../schemas/mapdiff-detailed-diagnostics.yaml
