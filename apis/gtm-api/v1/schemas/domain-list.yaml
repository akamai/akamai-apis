additionalProperties: false
properties:
  domains:
    description: |
      The GTM domain names accessible to the current user.
    items:
      type: string
      minLength: 1
    type: array
  links:
    items:
      $ref: links.yaml
    type: array
    description: |
      Hypermedia links to help navigate through the result set, or to identify the current item whose `rel` is `self`.
required:
- domains
- links
type: object
