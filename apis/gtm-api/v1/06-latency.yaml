openapi: 3.0.0
info:
  title: Latency
  version: v1
servers:
- url: https://{hostname}/gtm-api/v1/reports
tags:
- description: |
    Reports on latency and loss of ping data by network agents in each data center for a GTM property.
  name: Latency
x-akamai:
  auth-type: EDGE_GRID
paths:
  /latency/domains/{domain}/datacenters/{datacenterId}:
    $ref: paths/datacenter-latency.yaml
