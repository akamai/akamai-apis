openapi: 3.0.0
info:
  title: Domain lists
  version: v1
servers:
- url: https://{hostname}/gtm-api/v1/reports
tags:
- description: |
    Lists domain names and details for their data centers.
  name: Domains
x-akamai:
  auth-type: EDGE_GRID
paths:
  /domain-list:
    $ref: paths/domains.yaml
  /domain-list/domain-names:
    $ref: paths/domain-names.yaml
  /domain-list/accounts/{accountId}/contract-types/{contractTypeId}/domain-names:
    $ref: paths/domain-names-for-account.yaml
  /domain-list/{domain}:
    $ref: paths/domain.yaml
