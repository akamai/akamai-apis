openapi: 3.0.0
info:
  title: Traffic
  version: v1
servers:
- url: https://{hostname}/gtm-api/v1/reports
tags:
- description: |
    Provides details on DNS requests for GTM properties in a five minute period.
  name: Traffic
x-akamai:
  auth-type: EDGE_GRID
paths:
  /traffic/datacenters-window:
    $ref: paths/traffic-datacenters-window.yaml
  /traffic/properties-window:
    $ref: paths/traffic-properties-window.yaml
  /traffic/domains/{domain}/datacenters/{datacenterId}:
    $ref: paths/datacenter-traffic.yaml
  /traffic/domains/{domain}/properties/{property}:
    $ref: paths/property-traffic.yaml
