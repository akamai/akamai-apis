description: |-
  Pass the `ETag` response header back as `If-Match` in write requests to ensure you don't overwrite edits by any other API clients. See [Concurrency control](ref:concurrency-control) for details.
  
example: '"a9dfe78cf93090516bde891d009eaf57"'
x-akamai:
  file-path: headers/Etag.yaml
schema:
  type: string
