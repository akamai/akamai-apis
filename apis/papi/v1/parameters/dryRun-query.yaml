name: dryRun
in: query
description: |-
  With the `validateRules` query parameter also enabled, allows for a _dry run_ in order to gather any possible errors without saving the rule tree. See [Validation best practices](ref:validation-best-practices) for more guidance.
  
x-akamai:
  file-path: parameters/dryRun-query.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
