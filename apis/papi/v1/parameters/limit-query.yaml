name: limit
in: query
description: |-
  For paginated responses, specifies the number of results to include on each page.
  
x-akamai:
  file-path: parameters/limit-query.yaml
required: false
schema:
  type: integer
  example: 50
  minimum: 1
