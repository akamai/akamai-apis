name: activatedOn
in: query
description: |-
  If present, returns the latest version activated on the given network, either `STAGING` or `PRODUCTION`.
  
x-akamai:
  file-path: parameters/activatedOn-query.yaml
required: false
schema:
  type: string
  example: STAGING
  enum:
  - STAGING
  - PRODUCTION
