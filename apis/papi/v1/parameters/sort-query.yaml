name: sort
in: query
description: |-
  Sort the results based on the `cnameFrom` value, either `hostname:a` for ascending or `hostname:d` for descending order. The default is `hostname:a`.
  
x-akamai:
  file-path: parameters/sort-query.yaml
required: false
schema:
  type: string
  example: hostname:a
  default: hostname:a
  enum:
  - hostname:a
  - hostname:d
