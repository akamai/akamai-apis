name: network
in: query
description: |-
  Network of activated hostnames, either `STAGING` or `PRODUCTION`.
  
x-akamai:
  file-path: parameters/network-query.yaml
required: false
schema:
  type: string
  example: PRODUCTION
  enum:
  - PRODUCTION
  - STAGING
