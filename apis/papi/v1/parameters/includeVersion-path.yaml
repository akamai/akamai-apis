name: includeVersion
in: path
description: |-
  Include's incremental version number.
  
x-akamai:
  file-path: parameters/includeVersion-path.yaml
required: true
schema:
  type: integer
  example: 3
