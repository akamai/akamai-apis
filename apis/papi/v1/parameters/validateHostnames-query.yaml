name: validateHostnames
in: query
description: |-
  When `false`, skips validation tests that would identify potential hostname-related problems within the response object's `errors` and `warnings` arrays. See [Property hostname errors](ref:validation-errors) for details on relevant error feedback. See [Validation best practices](ref:validation-best-practices) for guidance on when to defer validation.
  
x-akamai:
  file-path: parameters/validateHostnames-query.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
