name: includeId
in: path
description: |-
  Unique identifier for the include. See [ID prefixes](ref:id-prefixes) for details on omitting the value's `inc_` prefix.
  
x-akamai:
  file-path: parameters/includeId-path.yaml
required: true
schema:
  type: string
  example: inc_175780
