name: cpcodeId
in: path
description: |-
  Unique identifier for the CP code. See [ID prefixes](ref:id-prefixes) for details on omitting the value's `cpc_` prefix.
  
x-akamai:
  file-path: parameters/cpcodeId-path.yaml
required: true
schema:
  type: string
  example: cpc_1234567
