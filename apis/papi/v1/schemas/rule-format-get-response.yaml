id: resource:/descriptors/papi/v1/schemas/GetRuleFormatResponse.json#
type: object
description: |-
  A common outer wrapping object structure. It provides additional contextual details for a set of requested data. The data itself is available under various inner members' `items` array.
  
x-akamai:
  file-path: schemas/rule-format-get-response.yaml
additionalProperties: false
properties:
  ruleFormats:
    type: object
    description: |
      The set of requested rule formats, strings available within an `items` array.
    additionalProperties: false
    properties:
      items:
        type: array
        description: |
          The supported rule formats.
        items:
          type: string
$schema: http://json-schema.org/draft-04/schema#
