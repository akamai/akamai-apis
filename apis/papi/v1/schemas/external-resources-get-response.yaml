id: resource:/descriptors/papi/v1/schemas/GetExternalResourcesResponse.json#
type: object
description: |-
  A common outer wrapping object structure. It provides additional contextual details for a set of requested data. The data itself is available under various inner members' `items` array.
  
x-akamai:
  file-path: schemas/external-resources-get-response.yaml
additionalProperties: false
properties:
  externalResources:
    type: object
    description: |
      The map of available external resources, available within the object.
$schema: http://json-schema.org/draft-04/schema#
