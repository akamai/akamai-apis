id: resource:/descriptors/papi/v1/schemas/SearchPropertyVersionsBySingleValueRequestV0.json#
javaType: com.akamai.luna.papi.model.SearchFindByValueRequest
type: object
description: |-
  A query to search currently active properties or includes. You can only include one query member in the POST request.
  
x-akamai:
  file-path: schemas/search-include-or-property-versions.yaml
minProperties: 1
maxProperties: 1
properties:
  edgeHostname:
    type: string
    description: |
      Search for property versions active on a specific edge hostname.
  hostname:
    type: string
    description: |
      Search for property versions active on a specific hostname.
  includeName:
    type: string
    description: |
      Search for includes by name.
  propertyName:
    type: string
    description: |
      Search for properties by name.
$schema: http://json-schema.org/draft-04-schema#
