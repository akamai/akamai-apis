description: |-
  Represents a float value field.
  
x-akamai:
  file-path: schemas/type-floatField.yaml
title: floatField
allOf:
- $ref: type-abstractNumberField.yaml
- additionalProperties: false
  required:
  - type
  properties:
    type:
      description: |
        Field value type, `float` in this case.
      enum:
      - float
    sizeFloat:
      type: number
      description: |
        Number digits after zero.
