type: object
description: |-
  This specifies a collection of alert summaries, all of which appear in the `data` array.
  
x-akamai:
  file-path: schemas/summaries.yaml
additionalProperties: false
required:
- data
properties:
  data:
    type: array
    description: |
      Summary of configuration and recent firing information.
    minItems: 0
    items:
      $ref: summary.yaml
