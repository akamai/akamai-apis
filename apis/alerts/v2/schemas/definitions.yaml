type: object
description: |-
  Specifies a collection of alert definitions, all of which appear in the `data` array.
  
x-akamai:
  file-path: schemas/definitions.yaml
additionalProperties: false
required:
- data
properties:
  data:
    type: array
    description: |
      Alert definitions.
    minItems: 1
    items:
      $ref: definition.yaml
