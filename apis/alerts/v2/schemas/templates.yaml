type: object
description: |-
  This specifies a collection of templates, all of which appear in the `data` array.
  
x-akamai:
  file-path: schemas/templates.yaml
additionalProperties: false
required:
- data
properties:
  data:
    type: array
    description: |
      Alert template describing a type of alert that can be defined or configured for monitoring.
    minItems: 1
    items:
      $ref: template.yaml
