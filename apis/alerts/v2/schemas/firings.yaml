type: object
description: |-
  This specifies a collection of alert firings, all of which appear in the `data` array.
  
x-akamai:
  file-path: schemas/firings.yaml
additionalProperties: false
required:
- data
properties:
  data:
    type: array
    description: |
      Alert firings.
    minItems: 0
    items:
      $ref: firing.yaml
