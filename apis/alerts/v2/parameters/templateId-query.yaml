name: templateId
in: query
description: |-
  When the `type` is either `cpcode` or `httpErrorCode`, restricts the range of data to what a specific template supports. For example, status codes in the 3xx range may not be relevant for a template that focuses on 4xx-5xx errors.
  
x-akamai:
  file-path: parameters/templateId-query.yaml
schema:
  type: string
  example: s@134
