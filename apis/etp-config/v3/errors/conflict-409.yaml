description: |-
  [Conflict with current state of resource](ref:409). Use the `ETag` string returned in the response header as `If-Match` in write requests to avoid overwriting edits by any other API clients. See [Concurrency control](ref:concurrency-control) for details.
  
x-akamai:
  file-path: errors/conflict-409.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/conflict-409.json
    schema:
      $ref: ../schemas/problem.yaml
