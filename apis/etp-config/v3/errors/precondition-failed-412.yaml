description: |
  [Precondition failed](ref:412). Use the `x_etag` string returned in the response header as `If-Match` in write requests to avoid overwriting edits by any other API clients. See [Concurrency control](ref:concurrency-control) for details.
content:
  application/problem+json:
    example:
      $ref: ../examples/precondition-failed-412.json
    schema:
      $ref: ../schemas/problem-412.yaml
