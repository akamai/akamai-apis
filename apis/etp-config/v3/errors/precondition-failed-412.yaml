description: |-
  [Precondition failed](ref:412). Use the `ETag` string returned in the response header as `If-Match` in write requests to avoid overwriting edits by any other API clients. See [Concurrency control](ref:concurrency-control) for details.
  
x-akamai:
  file-path: errors/precondition-failed-412.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/precondition-failed-412.json
    schema:
      $ref: ../schemas/problem.yaml
