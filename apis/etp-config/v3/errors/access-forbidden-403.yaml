description: |
  Access is forbidden. See [Get started](ref:get-started) for help setting up your permissions.
content:
  application/problem+json:
    example:
      $ref: ../examples/problem-response-403.json
    schema:
      $ref: ../schemas/problem.yaml
