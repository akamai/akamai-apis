description: |
  [Resource already onboarded](ref:406).
content:
  application/problem+json:
    example:
      $ref: ../examples/contract-already-onboarded-error-406.json
    schema:
      $ref: ../schemas/onboard-response.yaml
