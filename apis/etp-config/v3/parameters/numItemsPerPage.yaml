name: numItemsPerPage
in: query
description: |-
  The number of items per page, 50 by default.
  
x-akamai:
  file-path: parameters/numItemsPerPage.yaml
schema:
  type: integer
  example: 25
  default: 50
