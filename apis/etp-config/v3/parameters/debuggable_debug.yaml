name: debug
in: query
description: |-
  Indicates if the response includes ISO 8601 timestamp fields for debugging, `false` by default.
  
x-akamai:
  file-path: parameters/debuggable_debug.yaml
schema:
  type: boolean
  example: false
  default: false
