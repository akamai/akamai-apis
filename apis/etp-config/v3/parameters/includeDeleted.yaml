name: includeDeleted
in: query
description: |-
  Indicates if the result set includes deleted resources, `false` by default.
  
x-akamai:
  file-path: parameters/includeDeleted.yaml
schema:
  type: boolean
  example: false
  default: false
