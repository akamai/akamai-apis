name: include-certificate
in: query
description: |-
  When `true`, the response includes the certificate block, `false` by default.
  
x-akamai:
  file-path: parameters/certificateQueryParams_include-certificate.yaml
schema:
  type: boolean
  example: false
  default: false
