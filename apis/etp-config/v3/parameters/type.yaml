name: type
in: query
description: |
  The target list type to search.
schema:
  type: string
  example: LIST_TYPE_IP
  enum:
  - LIST_TYPE_IP
  - LIST_TYPE_DOMAIN
  - LIST_TYPE_INVALID
  - LIST_TYPE_FILEHASH
  - URL
