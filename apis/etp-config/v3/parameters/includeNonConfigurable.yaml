name: includeNonConfigurable
in: query
description: |-
  Indicates if the result set includes non-configurable resources, `false` by default.
  
x-akamai:
  file-path: parameters/includeNonConfigurable.yaml
schema:
  type: boolean
  example: false
  default: false
