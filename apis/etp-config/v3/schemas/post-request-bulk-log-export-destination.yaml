type: object
description: |
  A bulk log export destination, which supports Azure and Linode storage.
additionalProperties: true
properties:
  type:
    type: string
    description: |
      The cloud storage destination type. Either `AzureBlob` or `LinodeS3`.
    enum:
    - AzureBlob
    - LinodeS3
  parameters:
    oneOf:
    - $ref: post-request-azure-bulk-log-export-destination.yaml
    - $ref: post-request-linode-bulk-log-export-destination.yaml
