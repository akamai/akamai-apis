type: array
description: |-
  Contains a list of regular expression entries for a data loss protection (DLP) dictionary.
  
x-akamai:
  file-path: schemas/regexCreate.yaml
minItems: 0
items:
  type: object
  additionalProperties: false
  required:
  - regex
  properties:
    regex:
      type: string
      description: |
        Contents of the regular expression entry.
      minLength: 1
