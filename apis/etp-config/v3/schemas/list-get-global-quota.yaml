type: object
description: |
  This object returns the global count of entries in all lists and the entries available to configure.
additionalProperties: false
required:
- count
- remainingCount
properties:
  count:
    type: integer
    description: |
      The total number of list items currently in your configuration.
    minimum: 0
  remainingCount:
    type: integer
    description: |
      The remaining number of list items that you can add to your configuration.
    minimum: 0
