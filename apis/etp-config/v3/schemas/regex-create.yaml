type: array
description: |
  Regular expression entries to add to a data loss protection (DLP) dictionary.
minItems: 0
items:
  type: object
  additionalProperties: false
  properties:
    regex:
      type: string
      description: |
        Contents of the regular expression entry.
      minLength: 1
