type: object
description: |-
  Lists the proxy credential creation properties.
  
x-akamai:
  file-path: schemas/proxyCredentialCreate.yaml
additionalProperties: false
required:
- password
- username
properties:
  password:
    type: string
    description: |
      The password encoded in Base64 format.
    format: byte
  username:
    type: string
    description: |
      Name of proxy credential user.
    minLength: 1
