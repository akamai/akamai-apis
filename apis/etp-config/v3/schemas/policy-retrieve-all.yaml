type: array
description: |
  The security policy to assign to a site. You can configure the accepted use policy and set responses for known and suspected threats under specified categories.
minItems: 0
items:
  $ref: policy-retrieve-specific.yaml
