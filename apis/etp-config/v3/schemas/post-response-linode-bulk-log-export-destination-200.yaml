type: object
description: |
  Linode export destination properties.
additionalProperties: false
properties:
  url:
    type: string
    description: |
      The storage bucket URL.
  encryptionToken:
    type: string
    description: |
      The encryption token.
  accessKeyId:
    type: string
    description: |
      The access key ID.
  destinationPath:
    type: string
    description: |
      The storage bucket name.
  accessKeySecret:
    type: string
    description: |
      The access key secret.
