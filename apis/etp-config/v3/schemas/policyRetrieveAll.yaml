type: array
description: |-
  This object contains the security policy to assign to a site. You can configure the accepted use policy and set responses for known and suspected threats under specified categories.
  
x-akamai:
  file-path: schemas/policyRetrieveAll.yaml
minItems: 0
items:
  $ref: policyRetrieveSpecific.yaml
