type: array
description: |-
  Contains a custom security list of domains and IP addresses, with a confidence level for each.
  
x-akamai:
  file-path: schemas/listRetrieveAll.yaml
minItems: 0
items:
  $ref: listRetrieveSpecific.yaml
