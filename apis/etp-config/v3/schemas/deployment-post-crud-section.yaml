type: object
description: |
  The set of deployed sites, policies, lists, and custom responses.
additionalProperties: false
required:
- sites
- subLocations
- policies
- honeypots
- customlists
- customPatterns
properties:
  sites:
    type: array
    description: |
      Site IDs in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  subLocations:
    type: array
    description: |
      Sublocation IDs in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  policies:
    type: array
    description: |
      Policy IDs in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  honeypots:
    type: array
    description: |
      Custom response IDs in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  customlists:
    type: array
    description: |
      The set of custom list IDs in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  customPatterns:
    type: array
    description: |
      The set of custom dictionary patterns in the deployment.
    minItems: 0
    items:
      $ref: resource-id-only.yaml
  globalSettings:
    type: object
    description: |
      The global settings updated for the deployment.
    additionalProperties: false
    properties:
      id:
        type: integer
        description: |
          The unique ID for the global settings modified.
        minimum: 0
  whitelists:
    type: array
    description: |
      The allowlists removed from the deployment.
    minItems: 0
    items:
      type: string
      minLength: 1
  blacklists:
    type: array
    description: |
      The blocklists removed from the deployment.
    minItems: 0
    items:
      type: string
      minLength: 1
