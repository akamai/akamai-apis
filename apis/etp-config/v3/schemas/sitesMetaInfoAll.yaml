type: array
description: |-
  Specifies IP addresses and CIDR blocks of a region or geographic area for an assigned policy.
  
x-akamai:
  file-path: schemas/sitesMetaInfoAll.yaml
minItems: 0
items:
  $ref: siteRetrieveSpecific.yaml
