type: object
description: |
  Linode bulk log export destination properties.
title: Linode
additionalProperties: false
required:
- encryptionToken
- accessKeyId
- destinationPath
- accessKeySecret
- url
properties:
  url:
    type: string
    description: |
      The bucket URL.
  encryptionToken:
    type: string
    description: |
      The encryption token.
  accessKeyId:
    type: string
    description: |
      The access key ID.
  destinationPath:
    type: string
    description: |
      The storage bucket name.
  accessKeySecret:
    type: string
    description: |
      The access key secret.
