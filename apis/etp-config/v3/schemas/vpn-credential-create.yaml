type: object
description: |
  The properties for VPN credential creation.
additionalProperties: false
required:
- ikeId
properties:
  ikeId:
    type: string
    description: |
      Internet key exchange (IKE) identifier of the VPN credential.
    minLength: 1
  encPsk:
    type: string
    description: |
      The password encoded in Base64 format.
    format: byte
