description: |
  Use the `ETag` string returned in the response header as `If-Match` in write requests to avoid overwriting edits by any other API clients. \nSee [Concurrency control](ref:concurrency-control) for details.
x-akamai:
  file-path: headers/ETag.yaml
schema:
  type: string
  example: '"a9dfe78cf93090516bde891d009eaf57"'
