type: object
description: |-
  Provides information about the stream version key that consists of the stream's identifier.
  
x-akamai:
  file-path: schemas/StreamUpdate.yaml
additionalProperties: false
properties:
  streamVersionKey:
    type: object
    description: |
      Specifies the primary key consisting of the stream and version identifiers.
    additionalProperties: false
    properties:
      streamId:
        type: integer
        description: |
          Identifies the stream.
      streamVersionId:
        type: integer
        description: |
          Identifies the version of the stream.
