type: array
description: |-
  Provides a list of delivery products that you can enable log collection for and the groups that have permission to access to these products. It also specifies a list of data set templates available for each product.
  
x-akamai:
  file-path: schemas/Products.yaml
items:
  $ref: Product.yaml
