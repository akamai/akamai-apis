type: object
description: |-
  Provides information about a connector type that you can use as a destination for log delivery in a stream.
  
x-akamai:
  file-path: schemas/ConnectorType.yaml
additionalProperties: false
properties:
  connectorType:
    type: string
    description: |
      Specifies the connector type, either `AZURE`, `S3`, `DATADOG`, `SPLUNK`, `GCS`, `HTTPS`, `SUMO_LOGIC`, `ORACLE_CLOUD_STORAGE`, or `LOGGLY`.
    enum:
    - AZURE
    - S3
    - DATADOG
    - SPLUNK
    - GCS
    - HTTPS
    - SUMO_LOGIC
    - ORACLE_CLOUD_STORAGE
    - LOGGLY
  connectorTypeName:
    type: string
    description: |
      A name of the connector type.
