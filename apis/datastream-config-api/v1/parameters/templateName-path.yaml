name: templateName
in: path
description: |-
  The name of the data set template that you want to use in your stream configuration. Currently, `EDGE_LOGS` is the only data set template available.
  
x-akamai:
  file-path: parameters/templateName-path.yaml
required: true
schema:
  type: string
  example: EDGE_LOGS
  enum:
  - EDGE_LOGS
