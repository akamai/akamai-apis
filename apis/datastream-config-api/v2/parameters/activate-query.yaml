name: activate
in: query
description: |-
  Activates the stream at the time of the request, `false` by default. When you [Edit a stream](ref:put-stream) or [Patch a stream](ref:patch-stream) that is active, you need to set this member to `true`.
  
x-akamai:
  file-path: parameters/activate-query.yaml
schema:
  type: boolean
  example: true
  default: false
