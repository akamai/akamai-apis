name: productId
in: query
description: |-
  Uniquely identifies the product, and allows listing data sets available for that product. In the [List data set fields](ref:get-dataset-fields) operation, you can provide one `productId` per API call.
  
x-akamai:
  file-path: parameters/productId-query.yaml
schema:
  type: string
  example: Adaptive_Media_Delivery
