type: object
description: |-
  The list of DataStream 1 stream IDs for which you need to generate the payload compatible with DataStream 2.
  
x-akamai:
  file-path: schemas/MigPreparePayloadRequest.yaml
additionalProperties: false
properties:
  streamIds:
    type: array
    description: |
      Identifies the stream.
    items:
      type: integer
