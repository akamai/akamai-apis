type: object
description: |
  Lists the data set fields that the stream logs.
additionalProperties: false
required:
- datasetFields
properties:
  datasetFields:
    type: array
    description: |
      Lists the data set groups and fields that the stream logs.
    items:
      $ref: ds-field.yaml
