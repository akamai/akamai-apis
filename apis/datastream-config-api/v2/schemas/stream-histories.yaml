type: array
description: |
  Details of the latest stream configuration version. Data includes properties, data set fields, destination setup, activation status, and log format.
items:
  $ref: stream-detail.yaml
