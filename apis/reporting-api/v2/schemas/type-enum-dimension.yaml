type: string
description: |
  Type of dimension values, either `STRING`, `LONG`, `DOUBLE`, `TIMESTAMP_SEC`, `TIMESTAMP_MS`, or `DATE_ISO8601`.
enum:
- STRING
- LONG
- DOUBLE
- TIMESTAMP_SEC
- TIMESTAMP_MS
- DATE_ISO8601
