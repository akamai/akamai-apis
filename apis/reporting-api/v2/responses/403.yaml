description: |
  You are unauthorized to make calls to the requested report.
content:
  application/problem+json:
    example:
      $ref: ../examples/403.json
    schema:
      $ref: ../schemas/error.yaml
