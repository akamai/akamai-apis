description: |
  Results are delayed. The `Location` header provides a redirect link to get the results, which you can call after the number of seconds in the `Retry-After` header.
headers:
  Location:
    $ref: ../headers/location.yaml
  Retry-After:
    $ref: ../headers/retry-after.yaml
