description: |
  The request cannot be processed, since its cost exceeds the maximum request cost limit.
content:
  application/problem+json:
    example:
      $ref: ../examples/422.json
    schema:
      $ref: ../schemas/error.yaml
