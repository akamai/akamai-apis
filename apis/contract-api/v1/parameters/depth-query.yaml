name: depth
in: query
description: |
  Returns a specific set of contracts. Select `TOP` to return only parent contracts, or `ALL` to return both parent and child contracts.
schema:
  type: string
  example: TOP
  default: TOP
  enum:
  - TOP
  - ALL
