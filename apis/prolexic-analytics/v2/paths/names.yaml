x-akamai:
  file-path: paths/names.yaml
  path-info: /customer/names
get:
  operationId: get-customer-names
  summary: Get active customer names
  tags:
  - Customer names
  description: |
    Retrieves contract and account details for customer names.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/customer-names-response.json
          schema:
            $ref: ../schemas/customer-names-response.yaml
    400:
      $ref: ../errors/error-400.yaml
    403:
      $ref: ../errors/error-403.yaml
    404:
      $ref: ../errors/error-404.yaml
