x-akamai:
  file-path: paths/critical-event-contract.yaml
  path-info: /critical-events/contract/{contract}
parameters:
- $ref: ../parameters/contract-path.yaml
get:
  operationId: get-critical-events-contract
  summary: List critical events
  tags:
  - Critical events
  description: |
    Retrieves a critical events list for a customer.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/critical-events-get.json
          schema:
            $ref: ../schemas/critical-event.yaml
    403:
      $ref: ../errors/error-403.yaml
    404:
      $ref: ../errors/error-404.yaml
