x-akamai:
  file-path: paths/attack-reports.yaml
  path-info: /attack-reports/contract/{contract}/start/{start}/end/{end}
parameters:
- $ref: ../parameters/end-path.yaml
- $ref: ../parameters/start-path.yaml
- $ref: ../parameters/contract-path.yaml
get:
  operationId: get-attack-reports
  summary: List attack reports
  tags:
  - Attack reports
  description: |
    Retrieves a list of attack reports for a customer within the specified time range.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/attack-reports-get.json
          schema:
            $ref: ../schemas/attack-reports.yaml
    403:
      $ref: ../errors/error-403.yaml
    404:
      $ref: ../errors/error-404.yaml
