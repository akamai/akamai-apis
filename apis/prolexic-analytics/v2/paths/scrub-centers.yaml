x-akamai:
  file-path: paths/scrub-centers.yaml
  path-info: /scrub-centers
get:
  operationId: get-scrub-centers
  summary: List active scrubbing centers
  tags:
  - Scrub centers
  description: |
    Provides a list of all active scrub centers. 
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/scrub-centers.json
          schema:
            $ref: ../schemas/scrub-centers.yaml
