x-akamai:
  file-path: paths/event-contract.yaml
  path-info: /events/contract/{contract}
parameters:
- $ref: ../parameters/contract-path.yaml
get:
  operationId: get-events-contract
  summary: List events
  tags:
  - Events
  description: |
    Retrieves an events list for a customer.
  parameters:
  - $ref: ../parameters/extended-events-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/events-get.json
          schema:
            $ref: ../schemas/event-response.yaml
    403:
      $ref: ../errors/error-403.yaml
    404:
      $ref: ../errors/error-404.yaml
