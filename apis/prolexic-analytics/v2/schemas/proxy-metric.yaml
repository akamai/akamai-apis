type: object
description: |-
  Valid proxy metrics.
  
x-akamai:
  file-path: schemas/proxy-metric.yaml
additionalProperties: false
required:
- metrics
properties:
  metrics:
    $ref: proxy-data.yaml
