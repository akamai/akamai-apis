type: object
description: |-
  The types of metrics you can request.
  
x-akamai:
  file-path: schemas/metrics-type-object.yaml
additionalProperties: false
required:
- connect
- fbm
- mitigationPre
- mitigationPost
- proxy
- routed
properties:
  connect:
    $ref: connect-and-routed-metric.yaml
  fbm:
    $ref: fbm-metric.yaml
  mitigationPost:
    $ref: mitigation-metric.yaml
  mitigationPre:
    $ref: mitigation-metric.yaml
  proxy:
    $ref: proxy-metric.yaml
  routed:
    $ref: connect-and-routed-metric.yaml
