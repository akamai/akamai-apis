type: object
description: |-
  Valid metrics for Flow Based Monitoring (FBM).
  
x-akamai:
  file-path: schemas/fbm-metric.yaml
additionalProperties: false
required:
- metrics
properties:
  metrics:
    $ref: fbm-metric-data.yaml
