type: object
description: |-
  Valid routed and connect metrics.
  
x-akamai:
  file-path: schemas/ConnectAndRoutedMetric.yaml
additionalProperties: false
required:
- metrics
properties:
  metrics:
    $ref: ConnectAndRoutedMetricsData.yaml#
