name: extended
in: query
description: |
  Set to `true` to include extended events data for top source IPs, top network prototols, and top destination ports. 
x-akamai:
  file-path: parameters/extended-events-query.yaml
required: false
schema:
  type: boolean
  example: true
