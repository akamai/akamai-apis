x-akamai:
  path-info: /domains/{domainName}/history
parameters:
- $ref: ../parameters/domain-name-path.yaml
get:
  operationId: get-domain-history
  summary: List domain history
  tags:
  - Domains
  description: |
    Returns a list of each change made to the domain.
  parameters:
  - $ref: ../parameters/page-query.yaml
  - $ref: ../parameters/page-size-query.yaml
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-domain-history-200.json
          schema:
            $ref: ../schemas/domain-history-response.yaml
