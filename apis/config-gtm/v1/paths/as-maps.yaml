x-akamai:
  path-info: /domains/{domainName}/as-maps
parameters:
- $ref: ../parameters/domain-name-path.yaml
get:
  operationId: get-as-maps
  summary: List AS maps
  tags:
  - AS maps
  description: |
    Returns a list of AS maps.
  responses:
    200:
      description: |
        Successful response.
      content:
        application/json:
          example:
            $ref: ../examples/list-as-maps-200.json
          schema:
            $ref: "../schemas/get-all-as-maps.yaml"
