name: domainModificationComments
in: query
description: |
  Specifies change descriptions for audit trail and domain's change history. The maximum is 4000 characters.
schema:
  type: string
  example: Load balancer policy change
  nullable: true
  maxLength: 4000
