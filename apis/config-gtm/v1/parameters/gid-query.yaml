name: gid
in: query
description: |-
  A unique identifier for the group. If your GTM client credential has access to more than one group, you need to specify which group to assign to the domain. For more information, see [API concepts](ref:api-concepts).
  
x-akamai:
  file-path: parameters/gid-query.yaml
schema:
  type: integer
  example: 15166
