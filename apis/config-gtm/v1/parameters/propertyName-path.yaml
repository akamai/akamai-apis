name: propertyName
in: path
description: |-
  A unique name for the property that when joined to the `domainName` with a dot separator, it forms a valid [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
  
x-akamai:
  file-path: parameters/propertyName-path.yaml
required: true
schema:
  type: string
  example: www
