name: bypassSafetyChecks
in: query
description: |
  When `true`, forces deletion to proceed even if safety checks detect potential issues. Defaults to `false`.
required: false
schema:
  type: boolean
  default: false
  example: false
