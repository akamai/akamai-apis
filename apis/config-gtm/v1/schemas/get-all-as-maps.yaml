type: object
additionalProperties: true
properties:
  items:
    type: array
    description: |
      The list of a domain's autonomous system maps.
    items:
      type: object
      $ref: as-map.yaml