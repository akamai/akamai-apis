type: object
x-akamai:
  file-path: schemas/delete-response.yaml
additionalProperties: false
properties:
  resource:
    type: string
    description: |
      Reflects the data you just deleted.
    nullable: true
  status:
    $ref: status.yaml
