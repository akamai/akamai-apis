type: object
additionalProperties: false
description: |
  The response returned when a bulk domain delete request is submitted.
properties:
  requestId:
    type: string
    description: |
      A unique identifier for the delete request. Use this to poll the request status or retrieve results.
    minLength: 1
