type: object
additionalProperties: true
properties:
  items:
    type: array
    description: |
      The list of a domain's geographic maps.
    items:
      type: object
      $ref: geographic-map.yaml