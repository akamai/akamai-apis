type: object
additionalProperties: false
description: |
  A paginated list of valid datacenter IDs available for use as virtual datacenters.
properties:
  items:
    type: array
    description: |
      The list of valid datacenter IDs for the requested page.
    items:
      type: integer
