type: object
additionalProperties: false
description: |
  The set of DNS record types supported for GTM properties.
properties:
  types:
    type: array
    description: |
      The list of supported DNS record type names, such as `A`, `AAAA`, `CNAME`, `MX`.
    items:
      type: string
