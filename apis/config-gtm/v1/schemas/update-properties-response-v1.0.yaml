type: object
additionalProperties: true
properties:
  resource:
    description: |
      The updated list of properties for the domain.
    $ref: get-all-properties.yaml
  status:
    description: |
      The status of the domain change request.
    $ref: status.yaml
