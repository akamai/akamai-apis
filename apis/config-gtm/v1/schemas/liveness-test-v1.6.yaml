title: livenessTest v1.6
allOf:
- $ref: liveness-test-v1.5.yaml
- type: object
  description: |
    Liveness tests, which run periodically to determine whether your servers respond to requests, as discussed in [API concepts](ref:api-concepts).
  additionalProperties: true
  properties:
    pre2023SecurityPosture:
      type: boolean
      description: |
        Indicates whether the system uses a security posture configuration from before 2023. Defaults to false.
      x-akamai:
        labels:
        - v1.6
      default: false
