name: limit
in: query
description: |
  Data rows to fetch starting from the most recent. The maximum number of items is `200`, `50` by default.
schema:
  type: integer
  example: 50
  default: 50
  minimum: 1
  maximum: 200
