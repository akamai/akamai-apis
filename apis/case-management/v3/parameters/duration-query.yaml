name: duration
in: query
description: |-
  Number of days in the past to fetch cases for. If omitted, the operation returns most recent cases matching values of the `type` and `limit` parameters.
  
x-akamai:
  file-path: parameters/duration-query.yaml
schema:
  type: integer
  example: 30
  minimum: 0
