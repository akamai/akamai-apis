type: object
description: |-
  Data needed to close the case.
  
x-akamai:
  file-path: schemas/request-case-closure-request.yaml
additionalProperties: false
properties:
  comment:
    type: string
    description: |
      Additional context for closing the case, `Please close the case` by default.
    default: Please close the case
    maxLength: 130000
