name: logType
in: query
description: |-
  Record type of the logs. Possible values are  either `R` for client requests to an edge server or `F` for forward requests from an edge server to the origin.
  
x-akamai:
  file-path: parameters/logType-query.yaml
schema:
  type: string
  example: R
  enum:
  - R
  - F
