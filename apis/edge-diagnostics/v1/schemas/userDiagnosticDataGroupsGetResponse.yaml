type: object
description: |-
  Collects all groups created to gather diagnostic data within the top-level object's `groups` member.
  
x-akamai:
  file-path: schemas/userDiagnosticDataGroupsGetResponse.yaml
additionalProperties: false
required:
- groups
properties:
  groups:
    type: array
    description: |
      Lists groups created to gather diagnostic data and their details.
    minItems: 0
    items:
      $ref: userDiagnosticDataGroupsPostResponse.yaml
