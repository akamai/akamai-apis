type: object
description: |-
  Reasons why `executionStatus` is `PARTIAL_SUCCESS`.
  
x-akamai:
  file-path: schemas/warning.yaml
additionalProperties: false
required:
- key
- message
properties:
  key:
    type: string
    description: |
      Unique key representing the warning message.
    minLength: 1
  message:
    type: string
    description: |
      Warning message for the `grep` request.
    minLength: 1
