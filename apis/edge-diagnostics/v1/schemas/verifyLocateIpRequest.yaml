type: object
description: |-
  Specifies the IP address you want to [verify](ref:post-verify-edge-ip) or get the [location data](ref:post-locate-ip) for, or [both](ref:post-verify-locate-ip).
  
x-akamai:
  file-path: schemas/verifyLocateIpRequest.yaml
additionalProperties: false
required:
- ipAddress
properties:
  ipAddress:
    type: string
    description: |
      IP address you want to get the data for.
    minLength: 1
