type: object
description: |-
  Collects details for operations with the `NOT_EXECUTED` status.
  
x-akamai:
  file-path: schemas/info.yaml
additionalProperties: false
required:
- key
- message
properties:
  key:
    type: string
    description: |
      Error key.
    minLength: 1
  message:
    type: string
    description: |
      Details about the error.
    minLength: 1
