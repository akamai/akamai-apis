type: object
description: |-
  Collects all GTM properties you have access to within a top-level `gtmProperties` array.
  
x-akamai:
  file-path: schemas/gtmPropertiesResponse.yaml
additionalProperties: false
required:
- gtmProperties
properties:
  gtmProperties:
    type: array
    description: |
      Lists GTM properties and their details.
    minItems: 0
    items:
      $ref: gtmProperty.yaml
