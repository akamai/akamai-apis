type: object
description: |-
  Line range of the behavior.
  
x-akamai:
  file-path: schemas/lineRange.yaml
additionalProperties: false
properties:
  end:
    type: integer
    description: |
      End line number of a behavior in `arlDataXml`.
    minimum: 1
  start:
    type: integer
    description: |
      Start line number of a behavior in `arlDataXml`.
    minimum: 1
