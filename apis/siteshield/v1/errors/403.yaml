description: |
  [Forbidden](ref:403). You don't have permission to access the resource.
content:
  application/problem+json:
    example:
      $ref: ../examples/http-problem-details-403.json
    schema:
      $ref: ../schemas/http-problem-details.yaml
