description: |
  The burst limit that specifies how many requests you can make in excess of the imposed rate limit. See [Rate limiting](ref:rate-limiting) for details.
schema:
  type: integer
  example: 100
