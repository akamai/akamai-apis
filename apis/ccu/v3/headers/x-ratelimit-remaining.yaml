description: |
  The number of requests left before exceeding the limit, which results in the [429](ref:429) error. See [Rate limiting](ref:rate-limiting) for details.
schema:
  type: integer
  example: 99
