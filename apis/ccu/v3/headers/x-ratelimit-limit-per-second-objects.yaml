description: |
  Maximum sustainable number of objects allowed each second per request. This depends on the object type in the given request. See [Rate limiting](ref:rate-limiting) for details.
schema:
  type: number
  example: 200.00
