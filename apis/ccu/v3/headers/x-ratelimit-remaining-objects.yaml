description: |
  The number of objects left before exceeding the limit, which results in the [429](ref:429) error. This depends on the object type in the given request. See [Rate limiting](ref:rate-limiting) for details.
schema:
  type: integer
  example: 10000
