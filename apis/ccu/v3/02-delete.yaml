openapi: 3.0.0
info:
  title: Deletions
  version: v3
servers:
- url: https://{hostname}/ccu/v3
tags:
- description: Purge by cache tag.
  name: Cache tag
- description: Purge by URL or by Akamai resource locator (ARL).
  name: URL/ARL
- description: Purge by content provider (CP) code.
  name: CP code
x-akamai:
  auth-type: EDGE_GRID
paths:
  /delete/cpcode/{network}:
    $ref: paths/delete-cpcode.yaml
  /delete/tag/{network}:
    $ref: paths/delete-tag.yaml
  /delete/url/{network}:
    $ref: paths/delete-url.yaml
