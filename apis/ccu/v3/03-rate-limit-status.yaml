openapi: 3.0.0
info:
  title: Rate limit status
  version: v3
servers:
- url: https://{hostname}/ccu/v3
tags:
- name: Rate limits
  description: |
    Groups operations that let you control rate limits.
x-akamai:
  auth-type: EDGE_GRID
paths:
  /rate-limit-status/{purge-type}:
    $ref: paths/rate-limit-status.yaml
