type: object
description: |-
  Filter criteria options to filter report data.
  
x-akamai:
  file-path: schemas/filtersCriteriaBoolean.yaml
additionalProperties: false
minProperties: 1
properties:
  in:
    type: array
    description: |
      Strings containing filter parameters to include in the report.
    minItems: 0
    items:
      type: boolean
  nin:
    type: array
    description: |
      Strings containing filter parameters to exclude from the report.
    minItems: 0
    items:
      type: boolean
