type: object
description: |
  Filter criteria options for onramp status. Either `Yes` or `No`.
additionalProperties: false
minProperties: 1
properties:
  in:
    type: array
    description: |
      Strings containing filter parameters to include in the report. Either `Yes` or `No`.
    minItems: 0
    items:
      $ref: ./filter-enums.yaml#/IS_ONRAMP
  nin:
    type: array
    description: |
      Strings containing filter parameters to exclude from the report. Either `Yes` or `No`.
    minItems: 0
    items:
      $ref: ./filter-enums.yaml#/IS_ONRAMP
