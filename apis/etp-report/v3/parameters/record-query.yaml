name: record
in: query
description: |-
  The target record from which to get blocklisted domains.
  
x-akamai:
  file-path: parameters/record-query.yaml
required: true
schema:
  type: string
  example: facebook.com
