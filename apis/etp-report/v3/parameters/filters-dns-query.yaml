name: filters
in: query
description: |-
  Filters report data using the [Filters](ref:filters) JSON object. You supply this object as the value after you define the filter parameters.
  
example:
  $ref: ../examples/filterQueryParam.json
x-akamai:
  file-path: parameters/filters-dns-query.yaml
required: true
schema:
  $ref: ../schemas/filtersDNSEvents.yaml
