name: dimension
in: query
description: |
  Optionally filter by a dimension of data. Refer to [Filters](ref:filters) for a complete list.
required: true
schema:
  type: string
  example: status
  enum:
  - encryptedSourceIp
  - encryptedMachineName
  - destinationPort
  - hostname
  - sourcePort
  - sinkholeIP
  - sinkholeId
  - requestId
  - status
