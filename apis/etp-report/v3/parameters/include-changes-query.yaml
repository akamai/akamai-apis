name: includeChanges
in: query
description: |
  Whether to include the `iocChanges` array in the response. Defaults to `true`.
required: false
schema:
  type: boolean
  example: false
  default: true
