name: recordType
in: query
description: |-
  Specifies the record type, either `DOMAIN` or `IP`.
  
x-akamai:
  file-path: parameters/recordType-query.yaml
required: true
schema:
  type: string
  example: DOMAIN
  enum:
  - DOMAIN
  - IP
