openapi: 3.0.0
info:
  title: DNS activity reports
  version: v3
servers:
- url: https://{hostname}/etp-report/v3
tags:
- description: |
    Generate reports on threats detected when accessing a malicious domain.
  name: DNS activity reports
x-akamai:
  auth-type: EDGE_GRID
paths:
  /configs/{configId}/dns-activities/aggregate:
    $ref: paths/dns-activities-aggregate.yaml
  /configs/{configId}/dns-activities/details:
    $ref: paths/dns-activities-details.yaml
  /configs/{configId}/dns-activities/time-series:
    $ref: paths/dns-activities-time-series.yaml
