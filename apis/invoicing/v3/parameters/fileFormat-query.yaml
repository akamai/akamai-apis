name: fileFormat
in: query
description: |-
  File format of the invoice. These are the available values: `PDF`, `CSV`, and `JSON`.
  
x-akamai:
  file-path: parameters/fileFormat-query.yaml
required: true
schema:
  type: string
  example: PDF
  enum:
  - PDF
  - CSV
  - JSON
