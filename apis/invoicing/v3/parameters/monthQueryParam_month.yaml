name: month
in: query
description: |-
  Selected billable usage period, expressed as an ISO 8601 datestamp (YYYY-MM).
  
x-akamai:
  file-path: parameters/monthQueryParam_month.yaml
required: true
schema:
  type: string
  example: 2020-07
