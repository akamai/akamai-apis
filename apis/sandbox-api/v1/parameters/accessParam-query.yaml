name: access
in: query
description: |-
  Indicates the access level of the request. Specify either `USER` to perform the request for the sandboxes that you can access, or `ACCOUNT` to perform the request for all sandboxes within the account.
  
x-akamai:
  file-path: parameters/accessParam-query.yaml
schema:
  type: string
  example: ACCOUNT
  default: USER
  enum:
  - USER
  - ACCOUNT
