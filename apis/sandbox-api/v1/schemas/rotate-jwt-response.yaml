type: object
x-akamai:
  file-path: schemas/rotate-jwt-response.yaml
additionalProperties: false
required:
- jwtToken
properties:
  jwtToken:
    type: string
    description: |
      Generates a new JSON Web Token for the specified sandbox instance.
