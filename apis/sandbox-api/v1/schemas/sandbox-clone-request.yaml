type: object
description: |-
  You can clone a sandbox based on a pre-configured sandbox if you have the `sandboxId`.
  
x-akamai:
  file-path: schemas/sandbox-clone-request.yaml
additionalProperties: false
properties:
  name:
    type: string
    description: |
      Descriptive name for the new sandbox.
