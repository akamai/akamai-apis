type: object
x-akamai:
  file-path: schemas/properties-get-response.yaml
additionalProperties: false
required:
- properties
properties:
  properties:
    type: array
    description: |
      The set of properties in this sandbox.
    minItems: 1
    items:
      description: |
        Detailed information on the property.
      $ref: Property.yaml
