type: array
description: |-
  As Adaptive Acceleration gathers real-user monitoring data, it identifies commonly loaded headers. These headers are targeted for preconnecting.
  
x-akamai:
  file-path: schemas/preconnectHeader.yaml
items:
  type: string
