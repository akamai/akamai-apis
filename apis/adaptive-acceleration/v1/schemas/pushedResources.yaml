type: array
description: |-
  List of URLs for which Adaptive Acceleration currently performs a server push.
  
x-akamai:
  file-path: schemas/pushedResources.yaml
items:
  type: string
  format: uri
