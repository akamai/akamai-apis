name: listType
in: query
description: |-
  Filters the output to lists of only the given type of network lists if provided, either `IP` or `GEO`.  This corresponds to the network list object's `type` member.
  
x-akamai:
  file-path: parameters/listType_listType.yaml
schema:
  type: string
  example: IP
  enum:
  - IP
  - GEO
