name: environment
in: path
description: |-
  The Akamai network on which this list is activated: `STAGING` or `PRODUCTION`.
  
x-akamai:
  file-path: parameters/environment-path.yaml
required: true
schema:
  type: string
  example: PRODUCTION
  enum:
  - STAGING
  - PRODUCTION
