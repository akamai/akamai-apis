description: |
  Forbidden. You don't have permission to perform that function on this network list. Check that your user has the required privileges.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
