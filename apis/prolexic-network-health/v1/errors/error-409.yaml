description: |
  [Target count limit exceeded](ref:409). Exceeds the maximum number of allowed monitored targets based on the tier configuration.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/problem.yaml
    example:
      $ref: ../examples/error-409.json