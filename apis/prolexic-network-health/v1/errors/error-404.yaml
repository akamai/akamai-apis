description: |
  [Target not found](ref:404). The requested monitored target doesn't exist.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/problem.yaml
    example:
      $ref: ../examples/error-404.json
