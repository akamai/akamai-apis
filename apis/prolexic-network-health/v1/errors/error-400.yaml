description: |
  [Bad request](ref:400). The request is invalid and contains malformed data.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/problem.yaml
    example:
      $ref: ../examples/error-400.json
