description: | 
  All configured targets.
type: object
properties:
  targets:
    description: |
      The configured targets.
    type: array
    items:
      $ref: target.yaml
additionalProperties: true